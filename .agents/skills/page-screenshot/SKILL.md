---
name: page-screenshot
description: 页面视觉截图与留档。当用户希望查看页面外观效果(布局/配色/样式)、保存页面截图到 07-bugs 留档时，调用全局 page-screenshot CLI 对指定页面截图。仅做视觉记录，不检查组件挂载与 JS 报错——功能/报错验证请用 page-selftest skill。
allowed-tools: PowerShell, Read, Glob, Grep
---

# 页面截图验证 Skill

## 触发条件

当用户提出以下任一意图时触发本 Skill（仅限视觉/留档意图）：

- "截个图"
- "截图验证一下"（指视觉外观）
- "看一下页面效果"
- "对 XXX.html 截图"
- "把 XXX 页面截个图"
- "保存页面截图到 07-bugs"

注意：若需求是"页面是否渲染成功""组件有没有挂载""有没有 JS 报错""页面为什么空白"
等功能验证，属于 page-selftest skill 的范围，**不要用本 skill**。

## 能力边界（与 page-selftest 的分工）

| 需求 | 使用 skill |
|---|---|
| 视觉外观确认、截图留档给用户查看 | 本 skill（page-screenshot） |
| 组件挂载、JS 报错、数据渲染等功能验证 | page-selftest |
| 功能正确性 + 视觉留档 | 先 page-selftest 验证，再本 skill 截图 |

注意：本环境 Read 工具无法显示图片，AI 无法直接"看"截图；截图主要用于留档与
用户人工查看，AI 的功能性判断请依赖 page-selftest 的结构化结果。

## 执行步骤

### Step 1：确定目标页面路径

根据用户输入，按优先级确定要截图的 HTML 文件：

1. **用户明确指定完整路径**：直接使用用户提供的相对路径或绝对路径。
2. **用户只提供页面名称或功能名**：
   - 在当前项目目录下搜索 `03-高保真页面/`（或用户提及的目录）
   - 使用 `Glob` 查找匹配文件，例如搜索 `**/*control*.html`
   - 若存在多个匹配，列出候选文件请用户确认
3. **用户未指定任何页面**：
   - 询问用户需要截图的页面路径

### Step 2：执行截图命令

使用 PowerShell 在当前项目目录下执行全局 CLI：

```powershell
page-screenshot --file "<目标文件路径>" --out "07-bugs" --prefix "<功能前缀>"
```

参数说明：
- `--file`：目标 HTML 文件路径（必需）
- `--out`：截图输出目录，默认为当前项目的 `07-bugs/`（与项目规范一致）
- `--prefix`：截图文件名前缀，建议从功能名提取，例如 `road_control`、`filing_form`
- `--width` / `--height`：可选，默认 1366×768
- `--root`：可选，静态服务器根目录，默认当前工作目录

### Step 3：读取并展示结果

1. 命令执行成功后，会输出截图保存路径。
2. 使用 `Read` 工具读取生成的 PNG 图片并展示给用户。
3. 向用户报告：截图保存路径、页面地址、截图分辨率。

### Step 4：失败处理

若命令执行失败：
1. 检查错误信息是否包含"未找到系统 Chrome 或 Edge"
   - 若是，提示用户安装 Chrome/Edge 或使用 `--chrome-path` 指定路径
2. 检查是否包含"文件不存在"
   - 若是，重新确认文件路径
3. 其他错误，展示原始错误信息并建议用户直接运行命令排查

## 路径推断规则

| 用户输入示例 | 推断行为 |
|---|---|
| "control.html" | 搜索 `**/*control*.html` |
| "道路资源动态管控" | 搜索 `**/*control*.html` 或匹配页面标题 |
| "road/catalog/control.html" | 直接使用该相对路径 |
| "03-高保真页面/road/catalog/control.html" | 直接使用该相对路径 |
| "截个图" | 询问用户具体页面 |

## 输出格式

执行完成后按以下格式回复：

```
已使用 page-screenshot 对 {页面路径} 进行浏览器截图验证。

- 截图保存路径：{完整路径}
- 页面访问地址：{http://127.0.0.1:8000/...}
- 分辨率：{宽度}x{高度}

[截图展示]
```

## 注意事项

- 本 Skill 仅调用已全局安装的 `page-screenshot` 命令，不需要 AI 自身具备截图能力。
- 截图过程会临时启动 Chrome/Edge 和本地 HTTP 服务，运行结束后自动清理。
- 若页面引用了上级目录资源（如 `../../common.js`），务必确保 `--root` 设置为项目根目录，否则相对路径资源可能加载失败。
