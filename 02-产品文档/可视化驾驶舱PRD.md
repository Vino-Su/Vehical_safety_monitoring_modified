# 可视化驾驶舱 PRD（产品需求文档）

| 属性 | 内容 |
| --- | --- |
| 产品名称 | 智能网联汽车安全监测平台 |
| 模块名称 | 可视化驾驶舱首页 |
| 文档版本 | V1.8 |
| 创建日期 | 2026-09-22 |
| 最后更新 | 2026-09-23 |
| 文档状态 | 原型验证 |

---
## 版本记录

| 版本   | 修改日期       | 修改人  | 修改内容                                                                                                                                                                                                                                                                              | 备注       |
| ---- | ---------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| V1.2 | 2026-09-22 | 产品设计 | 开放道路资源模块支持选中后切换道路资源地图视图；路段总数、开放路段、暂停路段作为互斥筛选项与地图联动，地图按区域展示路段数量聚合，点击聚合展示该区域路段矢量及道路状态图例；聚合视图精简地图内说明文字，取消“返回聚合”按钮，改为点击聚合标记或放大地图两种方式取消聚合，缩小地图后自动恢复区域聚合                                                                                                                                | 同日更新合并记录 |
| V1.8 | 2026-09-23 | 产品设计 | 车辆列表增加操作列和单车定位能力；用户点击车辆定位后关闭列表浮层，地图切换至车辆视图并放大居中到目标车辆，同时展示车辆上下文；车辆坐标暂不可用时保留列表并反馈无法定位；车辆详情增加实时数据与异常事件切换，展示单车事件明细并与当前车辆事件摘要保持同一口径 | 同日更新合并记录 |


---
## 1. 业务背景与使用角色
### 1.1 业务背景
- 模块定位：为监管值守和汇报场景提供道路资源、测试示范申请、车辆运行、异常事件和事故趋势的集中态势视图。
- 关联模块：引用开放道路管理、测试示范监管、车辆状态监控、异常状态事件和监测数据分析的数据；驾驶舱不替代原业务页面的办理能力。
### 1.2 使用角色

| 角色 | 该模块中的职责 | 客户端 |
| --- | --- | --- |
| 监管值守人员 | 查看全域态势、关注异常、定位区域 | 驾驶舱 |
| 管理汇报人员 | 查看资源、运行及事故趋势 | 驾驶舱 |

### 1.3 核心目标与价值

| 目标维度 | 说明 |
| --- | --- |
| 态势感知 | 在同一视图理解道路、申请、车辆、异常与事故情况 |
| 快速研判 | 指标切换后查看对应构成，区域选择后查看区域摘要 |
| 业务隔离 | 驾驶舱与 WEB 管理端独立呈现，不改变原有业务页面 |

---
## 2. 功能清单总览

| 编号 | 功能点 | 功能描述 | 优先级 | 本期建设 |
| --- | --- | --- | --- | --- |
| F01 | 全域态势总览 | 展示道路、申请、车辆、异常、实时事件、事故趋势和区域分布 | 高 | 是 |
| F02 | 指标构成切换 | 选择申请、车辆或异常指标后更新本模块构成图及中心口径；开放道路资源模块支持选中并联动地图筛选；异常事件指标和时间范围同步筛选地图异常车辆 | 高 | 是 |
| F03 | 区域下钻摘要 | 选择车辆或异常事件区域聚合查看对应车辆位置；选择道路资源区域聚合放大至该区域查看路段矢量，缩小地图后自动恢复区域聚合 | 高 | 是 |
| F04 | 地图图层 | 控制地图业务图层显隐 | 中 | 是 |
| F05 | 更多视图与详情浮层 | 开放道路资源提供道路列表入口；道路列表展示道路名称、所属区域、道路等级、车道数、开放状态、开放时段和适用业务场景（无人环卫、无人物流、无人出行、无人公交），支持按路段总数、开放道路、暂停道路筛选，并支持所属区域下拉筛选与分页浏览 | 中 | 是 |
| F06 | 申请列表浮层 | 测试示范申请提供申请列表入口；展示申请分类统计与申请明细，支持按业务类型、申请时间范围筛选和分页浏览 | 中 | 是 |
| F07 | 异常事件列表浮层 | 异常事件统计提供异常事件列表入口；展示事件分类统计与事件明细，支持按事件分类、发生时间范围筛选和分页浏览 | 中 | 是 |
| F08 | 车辆详情、视频与轨迹浮层 | 点击车辆点位或车辆列表中的定位操作打开对应车辆上下文；支持查看实时数据和单车异常事件，并从详情进入轨迹回放或右侧视频面板，视频与轨迹共享回放时间轴 | 高 | 是 |

**说明：** 本期为静态高保真原型，展示数据用于交互演示；正式数据来源、权限范围、统计周期、刷新频率和异常阈值需后续确认。
---
## 3. 功能详细设计
### 3.1 F01 全域态势总览
#### 3.1.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 值守人员快速掌握全市智能网联汽车运行态势 |
| 使用角色 | 监管值守人员、管理汇报人员 |
| 触发条件 | 打开驾驶舱首页 |
| 功能边界 | 展示聚合信息，不在驾驶舱办理审批、处置和配置 |
| 成功结果 | 页面呈现最新可用的各模块概况与更新时间 |

#### 3.1.3 数据与字段规则

| 字段 | 业务含义 | 数据来源 | 格式/口径规则 | 空值处理 |
| --- | --- | --- | --- | --- |
| 路段总数、开放路段、暂停路段 | 道路资源规模及开放状态 | 开放道路管理 | 以所选统计范围内有效路段计数 | 无数据展示“--” |
| 道路测试、示范应用、商业化试点 | 各业务申请量 | 准入申请 | 按申请业务类型计数 | 无数据展示“--” |
| 车辆总数、在线车辆、累计里程 | 车辆运行规模 | 车辆状态监控 | 在线以最近一次有效状态为准，里程单位由数据源提供 | 无数据展示“--” |
| 事件总数、预警事件、告警事件 | 异常事件数量 | 异常状态事件 | 预警与告警按事件分类计数 | 无数据展示“--” |
| 实时事件 | 最近异常事件的车辆、类型、时间 | 异常状态事件 | 按发生时间倒序 | 暂无事件时展示空状态 |
| 事故趋势 | 统计周期内事故数量变化 | 监测数据分析 | 按时间序列展示 | 暂无数据时不绘制假趋势 |

**异常信息模块呈现：** 异常事件统计图表与实时事件列表在同一模块容器内连续呈现，统计图表位于上方，实时事件列表位于下方；两者使用内部弱分隔保持层级，不额外展示“异常事件统计 / 实时事件”组合说明。

#### 3.1.5 交互流程
1. 用户打开首页，系统加载全域聚合数据及数据更新时间。
2. 加载成功后展示道路、申请、车辆、异常、事故和地图信息；单模块失败仅显示该模块错误，保留其他模块可用。
3. 数据延迟或连接中断时保留最近有效数据并显示对应状态；无权限时不暴露受限数据。
### 3.2 F02 指标构成切换
#### 3.2.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 在同一业务模块内切换关注指标并查看关联构成 |
| 使用角色 | 可查看该业务模块的驾驶舱用户 |
| 触发条件 | 选择申请、车辆或异常模块的指标，或选中开放道路资源模块并选择路段统计指标 |
| 功能边界 | 切换仅影响所属业务模块，不改变其他模块统计口径 |
| 成功结果 | 被选指标和关联构成图同步更新，其他模块保持原状态 |

#### 3.2.4 业务规则与状态变化

| 规则编号 | 触发条件 | 业务规则 | 数据/状态变化 |
| --- | --- | --- | --- |
| R01 | 选择指标 | 同一模块仅有一个当前选中项 | 更新选中指标与图表中心摘要 |
| R02 | 统计范围变化 | 只作用于所属模块 | 更新该模块指标与图表，不影响其他模块 |
| R03 | 选中开放道路资源模块 | 地图切换为道路资源视图，按区域展示路段数量聚合，并隐藏车辆与实时轨迹图层 | 更新地图业务视图和道路资源模块选中状态 |
| R04 | 选择路段统计指标 | 路段总数、开放路段、暂停路段为互斥筛选项 | 地图聚合数量及路段矢量仅展示对应筛选范围 |
| R05 | 地图缩放级别达到路段展示级别 | 道路资源视图自动取消区域聚合并展示当前筛选范围内的路段矢量；低于该级别时恢复区域聚合 | 地图在区域聚合与路段矢量之间切换，当前筛选条件保持 |
| R06 | 选中车辆实时监控模块 | 地图切换为车辆视图，按区域展示车辆数量聚合，并隐藏道路资源图层 | 更新地图业务视图和车辆实时监控模块选中状态 |
| R07 | 选择车辆总数、在线车辆或累计里程 | 三项为互斥筛选项；在线车辆仅统计非离线车辆，累计里程仅展示存在有效里程的车辆 | 更新车辆区域聚合数量、车辆点位范围和车辆图例，其他模块保持原状态 |
| R08 | 地图缩放级别达到车辆展示级别，或用户点击车辆区域聚合 | 车辆视图取消区域聚合并展示当前筛选范围内的具体车辆位置；车辆点位可查看车辆状态和里程摘要 | 地图切换至车辆点位视图，当前车辆筛选条件保持 |
| R09 | 地图缩放级别低于车辆展示级别 | 车辆视图恢复区域车辆数量聚合 | 地图恢复区域聚合，当前车辆筛选条件保持 |
| R15 | 选中异常事件统计模块 | 地图切换为异常事件视图，按当前统计时间范围和事件分类统计各区域异常事件量 | 隐藏车辆总览、道路及实时轨迹图层，展示异常事件区域聚合 |
| R16 | 切换异常事件统计时间范围 | 事件总数、预警事件、告警事件及地图聚合统一使用当前时间范围 | 更新异常事件指标、构成图和地图聚合数量 |
| R17 | 选择事件总数、预警事件或告警事件 | 三项为互斥筛选项；预警和告警按事件分类过滤，事件总数展示全部分类 | 更新地图区域聚合及车辆点位范围 |
| R18 | 地图缩放级别达到车辆展示级别，或用户点击异常事件区域聚合 | 异常事件视图取消区域聚合并展示存在匹配事件的车辆位置；车辆图标显示匹配事件数量角标 | 车辆按事件状态聚合为未完成或已完成并更新图例 |
| R19 | 车辆存在多个匹配事件 | 只要任一匹配事件状态为待处理、待审核或已退回，车辆归为未完成；仅当全部匹配事件状态为已完成时归为已完成 | 车辆颜色、图例和信息窗状态保持一致 |

#### 3.2.5 交互流程
1. 用户选择某模块的指标，系统将该项设为当前口径。
2. 系统更新关联构成和中心摘要，并保持模块占用空间不变。
3. 选中车辆实时监控模块或切换车辆指标时，系统同步刷新车辆区域聚合或车辆点位，并按人工驾驶、自动驾驶、离线展示状态图例。
4. 无可用构成数据时展示无数据状态，不以全零图表代替。
### 3.3 F03 区域下钻摘要
#### 3.3.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 快速查看地图区域内车辆位置与状态，或查看开放道路资源的路段空间分布 |
| 使用角色 | 可查看对应区域数据的驾驶舱用户 |
| 触发条件 | 选择车辆区域聚合标记，或在道路资源视图中选择道路区域聚合标记 |
| 功能边界 | 车辆视图展示当前筛选范围内的区域聚合和具体车辆位置；道路资源视图展示当前筛选范围内的路段矢量及状态信息，不在驾驶舱办理道路业务 |
| 成功结果 | 车辆视图展示区域车辆数量聚合，放大或点击聚合后展示车辆位置、状态和里程；道路资源视图展示当前筛选范围内的路段矢量、道路名称、长度、车道数和开放状态 |

#### 3.3.5 交互流程
1. 用户点击车辆区域聚合或道路资源区域聚合，系统读取该区域聚合信息。
2. 车辆视图放大地图至车辆展示级别并展示当前筛选范围内的车辆位置；道路资源视图放大至该区域并展示当前筛选范围内的路段矢量。
3. 用户点击车辆位置可查看车辆状态和累计里程；点击道路矢量可查看道路名称、区域、长度、车道数和开放状态；数据不足时展示可用字段和缺失提示。
4. 用户缩小地图后，车辆视图自动恢复区域聚合；道路资源视图自动恢复区域聚合标记。
### 3.4 F04 地图图层
#### 3.4.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 控制地图业务图层的显示与隐藏 |
| 使用角色 | 驾驶舱用户 |
| 触发条件 | 选择地图图层控制 |
| 功能边界 | 仅改变当前驾驶舱地图的显示状态 |
| 成功结果 | 地图图层状态更新，业务数据不改变 |

#### 3.4.3 数据与字段规则

| 字段 | 业务含义 | 数据来源 | 格式/口径规则 | 空值处理 |
| --- | --- | --- | --- | --- |
| 地图中心、缩放级别 | 当前地图视野状态 | 地图服务与驾驶舱配置 | 页面打开时使用默认全域视角，用户缩放或拖拽后保留当前视野 | 无法读取视野时恢复默认全域视角 |
| 行政区边界、测试车辆、异常事件、实时轨迹、开放道路 | 地图业务图层 | 关联业务模块 | 各图层独立控制显隐；异常事件按当前时间范围和事件分类筛选，开放道路按道路状态展示并受道路资源模块筛选条件影响 | 图层无数据时保留图层控制项并展示空状态 |

#### 3.4.5 交互流程
1. 用户打开驾驶舱，系统加载默认全域地图视野及可用业务图层。
2. 用户缩放或拖拽地图，系统更新当前视野并保持业务图层与点位相对位置一致。
3. 用户执行地图图层操作，系统切换对应图层显隐。
4. 用户选中开放道路资源模块，系统切换道路资源地图视图，并按当前路段筛选指标展示区域聚合。
5. 用户选中异常事件统计模块，系统切换异常事件地图视图，并按当前时间范围和事件分类展示区域异常事件聚合。
6. 用户选择车辆或异常事件区域聚合标记，系统放大地图并展示当前筛选范围内的车辆位置；异常车辆图标展示事件数量及未完成/已完成状态，缩小地图后自动恢复区域聚合。
7. 地图或图层加载失败时，保留其他驾驶舱模块可用，并展示地图不可用提示。
### 3.5 F05 道路列表浮层
#### 3.5.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 在驾驶舱内快速查看道路资源清单及开放状态分布 |
| 使用角色 | 监管值守人员、管理汇报人员 |
| 触发条件 | 用户点击开放道路资源标题后的道路列表入口 |
| 功能边界 | 仅提供道路资源查看、状态筛选与区域筛选，不在浮层内办理道路建档、编辑或状态管控 |
| 成功结果 | 系统展示道路清单及路段总数、开放道路、暂停道路统计，用户可按统计项和所属区域筛选清单，并按页查看道路 |

#### 3.5.3 数据与字段规则

| 字段 | 业务含义 | 数据来源 | 格式/口径规则 | 空值处理 |
| --- | --- | --- | --- | --- |
| 路段总数 | 当前道路资源范围内的有效路段总量 | 开放道路管理 | 统计全部有效路段 | 无数据展示“--” |
| 开放道路 | 当前道路资源范围内处于开放状态的路段数量 | 开放道路管理 | 仅统计开放状态路段 | 无数据展示“--” |
| 暂停道路 | 当前道路资源范围内处于暂停状态的路段数量 | 开放道路管理 | 仅统计暂停状态路段 | 无数据展示“--” |
| 道路清单字段 | 道路名称、所属区域、道路等级、车道数、开放状态、开放时段、适用业务场景 | 开放道路管理 | 按道路资源管理模块字段口径展示；开放状态枚举为开放、暂停 | 缺失字段展示“--” |
| 适用业务场景 | 道路支持的无人化业务场景 | 开放道路管理 | 枚举为无人环卫、无人物流、无人出行、无人公交；单条道路可同时支持多个场景 | 无场景数据时展示“--” |
| 所属区域筛选 | 按行政区缩小道路清单范围 | 开放道路管理 | 选项为全部区域及当前道路资源范围内的区域，与道路状态筛选叠加生效 | 无匹配结果时展示空结果提示 |
| 列表分页 | 道路清单每页展示数量及当前页码 | 驾驶舱配置 | 每页 10 条，展示结果总数、页码与翻页操作；筛选条件变化后回到第一页 | 无数据时列表展示空结果提示 |

#### 3.5.4 业务规则与状态变化

| 规则编号 | 触发条件 | 业务规则 | 数据/状态变化 |
| --- | --- | --- | --- |
| R10 | 打开道路列表浮层 | 默认展示全部有效路段，路段总数为当前选中统计口径，所属区域为全部区域，列表回到第一页 | 浮层打开并展示第一页道路清单 |
| R11 | 点击路段总数、开放道路或暂停道路统计 | 三项为互斥筛选项；开放道路仅展示开放状态路段，暂停道路仅展示暂停状态路段 | 更新当前筛选项和道路清单结果 |
| R12 | 在浮层内切换道路状态筛选 | 道路列表筛选与道路资源模块当前筛选口径保持同步 | 同步更新模块指标选中状态；道路地图处于道路视图时同步更新地图道路范围 |
| R13 | 选择所属区域下拉筛选 | 区域筛选与道路状态筛选叠加生效，仅展示同时满足两个条件的路段 | 更新清单结果并回到第一页 |
| R14 | 切换页码 | 筛选条件变化后回到第一页；当前页码超出结果总页数时收敛到最后一页，结果不足一页时仅展示单页且翻页不可用 | 更新当前页道路清单与页码状态 |

#### 3.5.5 交互流程
1. 用户点击开放道路资源标题后的道路列表入口。
2. 系统加载当前道路资源范围内的统计数据和道路清单，默认选中路段总数、所属区域为全部区域并展示第一页。
3. 用户点击路段总数、开放道路或暂停道路统计项，或在所属区域下拉中选择区域。
4. 系统按所选统计项与区域叠加刷新列表并回到第一页，列表按每页 10 条分页展示，用户可翻页查看其余道路。
5. 筛选项无匹配数据时展示空结果提示，其他统计项和区域筛选保持可用。
6. 用户点击浮层底部的关闭按钮后返回驾驶舱，已选道路筛选口径保留。
#### 3.5.6 异常处理

| 异常场景 | 触发条件 | 系统处理 | 用户反馈 | 恢复方式 |
| --- | --- | --- | --- | --- |
| 道路数据加载失败 | 无法读取道路资源数据 | 保留驾驶舱其他模块，列表区域不展示旧数据 | 提示道路列表暂不可用 | 关闭浮层后重新打开 |
| 筛选无结果 | 所选状态或区域下没有匹配路段 | 保留统计项、区域筛选和筛选状态，列表展示无结果提示 | 提示当前筛选条件下暂无道路 | 切换其他统计项或所属区域 |

---
### 3.6 F06 申请列表浮层
#### 3.6.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 在驾驶舱内快速查看测试示范申请的分类规模和申请明细 |
| 使用角色 | 监管值守人员、管理汇报人员 |
| 触发条件 | 用户点击测试示范申请标题后的申请列表入口 |
| 功能边界 | 仅提供申请信息查看、业务类型筛选、时间筛选和分页浏览，不在浮层内办理申请或审批 |
| 成功结果 | 系统展示申请分类统计及符合筛选条件的申请列表 |

#### 3.6.3 数据与字段规则

| 字段 | 业务含义 | 数据来源 | 格式/口径规则 | 空值处理 |
| --- | --- | --- | --- | --- |
| 申请分类统计 | 全部申请及各业务类型申请数量 | 测试示范监管 | 业务类型枚举为道路测试、示范应用、商业化试点 | 无数据展示“--” |
| 申请明细 | 申请编号、业务类型、申请类型、车辆数、流程状态、当前环节、申请时间 | 测试示范监管 | 流程状态和当前环节按申请当前态展示，申请时间按申请记录时间展示 | 缺失字段展示“--” |
| 业务类型筛选 | 按业务类型缩小申请范围 | 测试示范监管 | 选项为全部及三类业务类型，与时间范围筛选叠加生效 | 无匹配结果展示空结果状态 |
| 申请时间范围 | 按申请时间筛选 | 测试示范监管 | 支持开始时间和结束时间，包含边界日期 | 未设置一端时按另一端筛选 |
| 列表分页 | 申请明细分页浏览 | 驾驶舱配置 | 筛选条件变化后回到第一页；结果不足一页时禁用翻页操作 | 无数据保留筛选条件并展示空结果状态 |

#### 3.6.4 业务规则与状态变化

| 规则编号 | 触发条件 | 业务规则 | 数据/状态变化 |
| --- | --- | --- | --- |
| R20 | 打开申请列表浮层 | 默认选中全部申请，业务类型为全部，时间范围为空，列表回到第一页 | 浮层展示分类统计及第一页申请明细 |
| R21 | 点击申请分类统计 | 申请分类为互斥筛选项 | 更新申请明细范围并回到第一页 |
| R22 | 选择申请时间范围 | 开始时间、结束时间与业务类型筛选叠加生效；包含所选边界日期 | 更新申请明细范围并回到第一页 |
| R23 | 切换页码 | 当前页码超出结果总页数时收敛到最后一页 | 更新当前页申请明细；结果不足一页时翻页不可用 |

#### 3.6.5 交互流程
1. 用户点击测试示范申请标题后的申请列表入口。
2. 系统加载申请分类统计和申请明细，默认展示全部申请及第一页结果。
3. 用户点击业务类型统计项或选择申请时间范围，系统按筛选条件刷新申请明细并回到第一页。
4. 用户切换页码，系统展示对应申请明细；无匹配结果时展示空结果状态并保留当前筛选条件。
5. 用户点击关闭入口、遮罩或按下 Esc 后返回驾驶舱。
#### 3.6.6 异常处理

| 异常场景 | 触发条件 | 系统处理 | 用户反馈 | 恢复方式 |
| --- | --- | --- | --- | --- |
| 申请数据加载失败 | 无法读取申请统计或明细 | 保留驾驶舱其他模块，列表不展示旧数据 | 提示申请列表暂不可用 | 关闭浮层后重新打开 |
| 筛选无结果 | 业务类型和时间范围无匹配申请 | 保留筛选条件及分类统计 | 提示当前筛选条件下暂无申请 | 调整业务类型或时间范围 |

---
### 3.7 F07 异常事件列表浮层
#### 3.7.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 在驾驶舱内快速查看异常事件明细并定位关注事件 |
| 使用角色 | 监管值守人员、管理汇报人员 |
| 触发条件 | 用户点击异常事件统计标题后的异常事件列表入口，或点击实时事件区域的查看全部入口 |
| 功能边界 | 仅提供异常事件查看、分类筛选、时间范围筛选和分页浏览，不在浮层内处置事件 |
| 成功结果 | 系统展示异常事件分类统计及符合筛选条件的事件明细 |

#### 3.7.3 数据与字段规则

| 字段 | 业务含义 | 数据来源 | 格式/口径规则 | 空值处理 |
| --- | --- | --- | --- | --- |
| 事件分类统计 | 事件总数、预警事件、告警事件数量 | 异常状态事件 | 事件总数为筛选时间范围内全部事件；预警与告警按事件分类计数 | 无数据展示“--” |
| 事件明细 | 事件编号、事件分类、车牌号、企业、事件类型、事件描述、发生时间、处理状态 | 异常状态事件 | 按发生时间倒序；事件分类枚举为预警、告警；处理状态按事件当前状态展示 | 缺失字段展示“--” |
| 事件分类筛选 | 按预警或告警分类缩小事件范围 | 异常状态事件 | 事件总数、预警事件、告警事件为互斥筛选项 | 无匹配结果展示空结果状态 |
| 发生时间范围 | 按事件发生日期筛选 | 异常状态事件 | 支持开始日期和结束日期，包含所选边界日期；未设置一端时按另一端筛选 | 未设置时间范围时展示全部事件 |
| 列表分页 | 异常事件分页浏览 | 驾驶舱配置 | 筛选条件变化后回到第一页；结果不足一页时禁用翻页操作 | 无数据保留筛选条件并展示空结果状态 |

#### 3.7.4 业务规则与状态变化

| 规则编号 | 触发条件 | 业务规则 | 数据/状态变化 |
| --- | --- | --- | --- |
| R24 | 打开异常事件列表浮层 | 默认选中事件总数，时间范围为空，列表回到第一页 | 浮层展示三项分类统计及第一页事件明细 |
| R25 | 点击事件总数、预警事件或告警事件统计 | 三项为互斥筛选项；事件总数展示全部分类，预警和告警按分类过滤 | 更新列表结果并回到第一页 |
| R26 | 选择发生时间范围 | 开始日期、结束日期与事件分类筛选叠加生效，包含所选边界日期 | 更新三项统计和事件明细并回到第一页 |
| R27 | 切换页码 | 当前页码超出结果总页数时收敛到最后一页 | 更新当前页事件明细；结果不足一页时翻页不可用 |

#### 3.7.5 交互流程
1. 用户点击异常事件统计标题后的列表入口，或实时事件区域的查看全部入口。
2. 系统加载事件分类统计和事件明细，默认展示全部事件及第一页结果。
3. 用户点击事件分类统计项或选择发生时间范围，系统按筛选条件刷新统计与明细并回到第一页。
4. 用户切换页码，系统展示对应事件明细；无匹配结果时展示空结果状态并保留当前筛选条件。
5. 用户点击关闭入口、遮罩或按下 Esc 后返回驾驶舱。
#### 3.7.6 异常处理

| 异常场景 | 触发条件 | 系统处理 | 用户反馈 | 恢复方式 |
| --- | --- | --- | --- | --- |
| 事件数据加载失败 | 无法读取事件统计或明细 | 保留驾驶舱其他模块，列表不展示旧数据 | 提示异常事件列表暂不可用 | 关闭浮层后重新打开 |
| 筛选无结果 | 事件分类和时间范围无匹配事件 | 保留筛选条件及分类统计 | 提示当前筛选条件下暂无异常事件 | 调整事件分类或时间范围 |

---
### 3.8 F08 车辆详情、视频与轨迹浮层
#### 3.8.1 功能目标与范围

| 项目 | 说明 |
| --- | --- |
| 功能目标 | 在驾驶舱地图上快速查看单车上下文，并进入该车视频或轨迹回放 |
| 使用角色 | 监管值守人员、管理汇报人员 |
| 触发条件 | 地图放大到车辆点位层级后点击车辆 marker |
| 功能边界 | 驾驶舱仅提供车辆状态、单车异常事件查看、视频查看和轨迹回放；不在浮层内办理车辆档案、异常处置、逐事件回放或录像下载审批 |
| 成功结果 | 车辆详情浮层打开；用户可进入右侧视频面板或地图底部轨迹面板 |

#### 3.8.2 车辆详情浮层

车辆详情默认展示实时数据，支持切换查看该车异常事件；车牌号与所属企业、轨迹与视频入口在两种内容中保持可用。实时数据至少展示：

- 车牌号、所属区域、在线/离线状态；
- 驾驶模式、实时速度、加速度、档位、最后上报时间、累计里程；
- 当前异常事件数量及预警/告警摘要；
- “轨迹”“视频”两个快捷操作。

异常事件按发生时间倒序展示事件分类、事件类型、发生时间、发生位置、触发规则和处理状态；数量及预警/告警摘要与同一车辆事件集合一致。车辆视图展示该车全部事件；异常事件地图视图展示当前时间范围与事件分类筛选后的事件，数量与地图点位角标一致。无事件时提示“暂无异常事件记录”，字段缺失时显示“--”，不以其他车辆数据填充。

点击其他车辆时，当前选中车辆上下文切换为新车辆并默认展示实时数据；点击地图空白处或关闭按钮后关闭浮层。车辆列表定位及异常事件地图点位打开同一车辆详情；车辆身份无法映射时保留可用车辆信息和空事件状态，轨迹与视频入口不可用。

车辆列表支持按车牌号查看车辆并执行定位操作。定位成功后关闭列表浮层，地图切换至车辆视图并放大居中到目标车辆，打开该车辆上下文；列表筛选条件不改变车辆业务状态。

#### 3.8.3 轨迹回放
1. 点击车辆详情浮层中的“轨迹”后，地图绘制该车辆历史轨迹、已行驶轨迹和移动车辆标记。
2. 地图底部展开轨迹控制面板，支持日期、开始时间、结束时间查询。
3. 控制面板支持播放、暂停、`1x / 2x / 4x / 8x` 倍速和进度条拖动。
4. 控制面板展示当前时间、速度和驾驶模式。
5. 查询时间范围结束时间必须晚于开始时间；无效范围不刷新轨迹。
6. 轨迹回放过程中保留车辆选中状态；关闭轨迹面板时清理轨迹线和移动标记。

#### 3.8.4 视频面板
1. 点击车辆详情浮层中的“视频”后，地图右侧打开视频面板，默认进入实时视频。
2. 视频面板展示当前车辆、所属企业、连接状态和可用摄像头数量，支持前视、后视、左右侧、车内和环视摄像头切换。
3. 单路画面支持截图、本地录像和全屏；车辆离线或摄像头不可用时展示不可用状态，并保留面板关闭和返回地图操作。
4. 视频面板支持历史回放，按日期、开始时间和结束时间查询录像片段；回放状态需明确显示当前时间，实时状态可返回实时画面。
5. 关闭视频面板后返回地图，不改变当前地图筛选和车辆选择；切换车辆时同步更新视频上下文和地图聚焦位置。

#### 3.8.5 视频与轨迹时间轴联动
1. 轨迹进度条拖动或播放时，通过统一时间轴更新视频回放当前时间。
2. 视频回放进度条拖动时，轨迹移动标记定位到最接近的轨迹点。
3. 联动按绝对时间戳匹配，不按视频帧与轨迹点下标强行对齐。
4. 从轨迹面板点击“视频回看”时，视频面板进入同一日期和时间范围的历史回放。

#### 3.8.6 数据与异常

| 场景 | 系统处理 | 用户反馈 |
| --- | --- | --- |
| 车辆身份无法映射 | 保留车辆详情，视频/轨迹入口不可用 | 提示车辆数据暂不可用 |
| 车辆坐标无法读取 | 不执行地图移动，保留车辆列表及当前筛选结果 | 提示当前车辆暂无法定位 |
| 车辆离线 | 轨迹仍可查询历史数据，视频进入不可用状态 | 视频流不可用 |
| 无轨迹数据 | 不绘制轨迹，保留详情浮层 | 暂无符合条件的轨迹数据 |
| 视频时间范围无录像 | 保留查询条件和面板 | 当前时间范围暂无录像 |

#### 3.8.7 验收标准
1. 点击车辆点位可打开详情浮层，浮层不遮挡车辆关键状态信息。
2. 轨迹面板可查询并播放指定车辆的历史轨迹，进度条、速度和驾驶模式同步更新。
3. 视频面板可从详情浮层打开并完成实时/历史模式切换及摄像头切换。
4. 视频和轨迹拖动时间轴时能够互相定位，且不会产生循环刷新。
5. 视频面板、轨迹面板和详情浮层均符合驾驶舱深色 UI 设计规范，并在 16:9、32:9 视口内无明显遮挡或溢出。
6. 在车辆列表中执行定位操作后，列表浮层关闭，地图放大居中至目标车辆并展示该车辆上下文；目标车辆无有效坐标时给出不可定位反馈。
---
## 4. 业务规则汇总
### 4.1 数据口径与联动规则

| 触发字段 | 触发条件 | 联动行为 |
| --- | --- | --- |
| 业务模块指标 | 选择另一指标 | 仅更新本模块构成、摘要和选中状态；开放道路资源指标同时筛选地图道路 |
| 统计范围 | 选择时间范围 | 仅重算本模块当前指标与构成 |
| 地图区域 | 选择区域聚合 | 车辆视图放大至该区域并展示当前筛选范围内的车辆位置；道路资源视图放大至该区域并展示当前筛选范围内的路段矢量，缩小地图后恢复区域聚合，不改变全域累计数据 |
| 道路列表筛选 | 点击路段总数、开放道路或暂停道路统计，或选择所属区域 | 列表按状态与区域叠加筛选并回到第一页；道路地图处于道路视图时同步更新道路范围，关闭浮层后保留当前口径 |
| 申请列表筛选 | 点击申请业务类型统计或选择申请时间范围 | 列表按业务类型与时间范围叠加筛选并回到第一页，关闭浮层后返回驾驶舱 |
| 异常事件列表筛选 | 点击事件分类统计或选择发生时间范围 | 列表按事件分类与发生时间范围叠加筛选并回到第一页，关闭浮层后返回驾驶舱 |

### 4.2 数据状态规则
- 单模块加载、无数据、失败、延迟和权限受限相互区分；故障不影响其他模块。
- 实时数据应展示最近有效更新时间；演示原型不以模拟时钟充当真实业务刷新证明。
---
## 5. 关联模块说明

| 关联模块 | 关联关系 | 说明 |
| --- | --- | --- |
| 开放道路管理 | 提供道路资源和状态统计 | 与管理端采用一致的路段定义 |
| 测试示范监管 | 提供申请、车辆与区域分布 | 与准入及车辆监控口径一致 |
| 异常状态事件 | 提供预警、告警和实时事件 | 与事件分类及发生时间一致 |
| 监测数据分析 | 提供事故趋势 | 与事故统计时间口径一致 |
