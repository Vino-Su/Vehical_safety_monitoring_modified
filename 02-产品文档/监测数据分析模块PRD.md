| 属性 | 内容 |
| ------ | ---------- |
| 产品名称 | 智能网联汽车安全监测平台 |
| 模块名称 | 监测数据分析模块 |
| 文档版本 | V1.5 |
| 创建日期 | 2026-06-08 |
| 最后更新 | 2026-08-10 |
| 文档状态 | 待确认 |

---

# 版本记录

| 版本 | 修改日期 | 修改人 | 修改内容 | 备注 |
| ---- | -------- | ------ | -------- | ---- |
| V1.0 | 2026-06-08 | 产品经理 | 基于需求规格说明书 V1.4 和高保真页面完成首版 PRD | 覆盖 4 大子模块共 19 个页面 |
| V1.1 | 2026-06-08 | 产品经理 | 需求评审修正：F02 Tab 结构调整为 4Tab（告警明细合并入告警概览）；F06 单车维度补充自动驾驶/人工驾驶里程及最近测试日期字段；附录 A.8 补充事故/事件分类标签色值；查询类章节分页描述统一引用附录 A.1 | |
| V1.2 | 2026-06-10 | 产品经理 | F02 异常告警统计回归原始需求核心诉求：3Tab 架构（去掉分析报告 Tab 和预测模型、去掉东风悦享专用分析）；增加 " 设备异常/行为异常 " 融合分类维度；补全 " 影响范围 " 和 " 持续时长 " 核心指标；统计卡片调整为 8 个；图表区调整为 3 行 6 图；告警列表增加告警大类和持续时长列；详情弹窗补充影响范围和持续时长字段 | 回归原始需求，修正过度解读 |
| V1.3 | 2026-07-02 | 产品经理 | F02 异常告警统计与 F05 预警事件分析对齐异常状态事件模块（F15）分类体系：(1) F02 由 3Tab 简化为单页面，告警类型由 6 种调整为 4 种（临牌有效期/电子围栏越界/测试时段合规/安全员到岗），取消 " 设备异常/行为异常 " 大类和 " 告警级别 " 维度，统计卡片 8→6，图表新增企业告警排名 TOP10 和企业告警类型分布，编号改为 ALT04~ALT07 格式；(2) F05 废弃原 5 种 ADAS 预警类型，改为 F15 预警类 3 种（车辆运行异常/碰撞事件/车载终端异常），统计卡片 4→6，图表 3→4，编号改为 EW01~EW03 格式；(3) 功能清单、附录色值同步更新 | 对齐 F15 分类体系 |
| V1.4 | 2026-08-05 | 产品经理 | （1）管理端采用统一模块导航；页面上下文由当前模块和功能菜单确定，取消路径式面包屑及独立页面标题；原页面操作入口保持可用，并按当前查询或功能分组呈现；（2）F08 企业数据查询：两个 Tab 查询时间条件由独立日期选择器调整为单个日期范围选择框（条件命名为 " 日期范围 "），数据粒度由区间汇总调整为按日统计（列表增加 " 日期 " 字段，默认按日期倒序）；企业测试查询列表新增 " 示范应用里程/示范应用时长 " 字段；企业脱手率查询列表 " 脱离次数 " 更名为 " 脱手次数 "；两个 Tab 列表首列增加行复选框（支持表头全选当页），导出按附录 A.3 支持勾选导出；（3）F09 安全员数据查询：查询时间条件由独立日期选择器调整为单个日期范围选择框（条件命名为 " 日期范围 "），数据粒度由区间汇总调整为按日统计（列表增加 " 日期 " 字段，默认按日期倒序），列表首列增加行复选框，导出按附录 A.3 支持勾选导出；（4）F10 车辆牌照查询：查询条件调整为临牌号/VIN 码/企业名称/车辆类型/牌照状态（有效期内、已过期），列表字段调整为复选框/临牌号/VIN 码/企业名称/车辆类型/牌照状态/有效期起止/历史牌照（移除 " 车型 " 列，牌照状态术语统一为 " 有效期内/已过期 "），首列复选框支持勾选导出；（5）F10 车辆测试查询：查询条件调整为 VIN 码/临牌号/企业名称/日期范围，列表 " 开始时间/截止时间 " 合并为 " 日期 " 字段（按日粒度，默认按日期倒序），首列增加行复选框，导出按附录 A.3 支持勾选导出；（6）F10 车辆实时数据查询：列表字段调整为复选框/VIN 码/临牌号/企业名称/采集时间/车辆位置/驾驶模式/实时速度/加速度（车辆标识拆分为 VIN 码+临牌号+企业名称，移除行驶方向/设备编码/落库时间，车辆控制模式更名驾驶模式、车辆速度更名实时速度），移除 10 秒自动刷新与暂停刷新机制，首列复选框支持勾选导出；（7）F10 车辆实时数据查询筛选条件调整为 VIN 码/临牌号/企业名称/日期范围（移除设备编码、采集开始/采集截止）；（8）F10 车辆作业数据查询：查询条件调整为 VIN 码/临牌号/企业名称/日期范围，列表字段调整为复选框/VIN 码/临牌号/企业名称/日期/作业时长/作业里程/油耗/电耗（按日粒度，油耗/电耗拆分为两列，移除清扫面积与表格底部合计行，导出不再包含合计行）；（9）F11 设备数据查询：查询条件调整为 VIN 码/临牌号/企业名称/日期范围/设备编号/在线状态/SIM 卡号（设备 ID 更名设备编号，车辆标识拆分为 VIN 码+临牌号），列表字段调整为复选框/VIN 码/临牌号/企业名称/设备编号/设备版本号/在线状态/设备硬盘状态/SIM 卡号/安装日期（移除备注列，安装时间更名安装日期），首列复选框支持勾选导出；（10）F12 车辆故障查询：查询条件调整为 VIN 码/临牌号/企业名称/发生日期范围/故障码/故障码状态（故障状态更名故障码状态，发生时间起/止合并为发生日期范围），列表字段调整为复选框/VIN 码/临牌号/企业名称/设备编号/故障发生时间/故障更新时间/故障码/故障等级/故障描述/所属系统/故障码状态（移除备注列，新增故障等级/所属系统/设备编号），首列复选框支持勾选导出；（11）F12 故障等级枚举调整为 1 级~4 级（严重程度递增），故障码状态枚举调整为已消除/未消除；（12）F10 车辆脱手率查询：查询条件调整为 VIN 码/临牌号/企业名称/日期范围，列表 " 开始时间/截止时间 " 合并为 " 日期 " 字段（按日粒度，默认按日期倒序），首列增加行复选框，导出按附录 A.3 支持勾选导出 | — |
| V1.5 | 2026-08-10 | 产品经理 | F10 车辆实时数据查询与车辆运行监测统一数据口径：列表业务字段调整为临牌号、VIN码、所属企业、数据采集时间、经度、纬度、车辆驾驶模式、车辆速度、档位；新增查看详情，展示车辆动态、车身信号与远程控制完整字段集；保留日期范围查询、勾选导出和分页能力；筛选条件日期范围升级为精确到秒的时间范围选择；F11 设备数据查询与 F12 车辆故障查询的低频查询条件改为按需展开，收起不清除条件，查询和重置覆盖全部条件 | — |

---

# 名词解释

| 名词/缩写 | 解释                               |
| ----- | -------------------------------- |
| OCR   | 光学字符识别，用于从事故报告图片/PDF 中自动提取结构化字段信息 |
| VIN 码  | 车辆识别代号，17 位唯一编码，用于标识车辆身份          |
| RSU   | 路侧单元（Road Side Unit），部署在道路侧的通信设备 |
| T-Box | 车载远程信息处理终端，用于采集和上传车辆运行数据         |
| 脱手率   | 自动驾驶测试中，安全员脱离方向盘/接管的频率指标         |
| 干预率   | 安全员在自动驾驶测试中主动干预驾驶行为的频率指标         |

---

## 1. 业务背景与使用角色

### 1.1 业务背景
- 模块定位：监测数据分析模块基于测试示范全量数据，提供测试数据管理、查询检索、事故分析研判及三大专题（道路测试、示范应用、示范运营）的可视化分析功能
- 核心特点：以数据可视化驱动决策，所有分析图表均支持时间范围选择、维度筛选、图形导出及数据下钻
- 关联模块关系：
  - 测试示范监管模块：引用企业接入信息、车辆信息、安全员信息作为分析数据源
  - 平台管理模块：引用字典配置、日志记录等基础数据
  - 车辆监控模块：实时位置、告警数据、运行状态等通过消息队列推送至分析模块

### 1.2 使用角色

| 角色     | 该模块中的职责            | 客户端  |
| ------ | ------------------ | ---- |
| 政府监管人员 | 查看全局数据分析、事故研判、产业趋势 | Web 端 |
| 企业用户   | 查看本企业相关数据分析和查询结果   | Web 端 |
| 系统管理员  | 管理数据权限、查看审计日志      | Web 端 |

### 1.3 核心目标与价值

| 目标维度 | 说明 |
| -------- | ---- |
| 数据总览 | 一站式展示测试企业、车辆、里程、告警等关键指标 |
| 异常监测 | 实时监测告警状态、处理进度和趋势变化 |
| 事故分析 | 从解析、成因、特征、研判四个维度深度分析事故数据 |
| 产业洞察 | 分析企业增长、测试规模、场景渗透等产业发展指标 |
| 专题分析 | 按道路测试、示范应用、示范运营三个专题分维度分析 |
| 数据查询 | 支持企业、车辆、安全员、设备、故障等多维数据查询与导出 |

---

## 2. 功能清单总览

| 编号  | 功能点      | 功能描述                                                                                  | 优先级 | 本期建设 |
| --- | -------- | ------------------------------------------------------------------------------------- | --- | ---- |
| F01 | 测试数据总览   | 展示累计统计卡片、测试里程图表、当日测试情况、企业车辆统计表、实时车辆动态列表                                               | P0  | 是    |
| F02 | 异常告警情况统计 | 单页面架构，聚焦 F15 告警类 4 类事件（临牌有效期/电子围栏越界/测试时段合规/安全员到岗），含 6 统计卡片、6 图表（含企业排名 + 企业类型分布）、明细列表、Leaflet 地图 | P0  | 是    |
| F03 | 违法事故统计   | 事故统计卡片、事故类型分布饼图、区域分布柱状图、事故趋势图、事故列表                                                    | P0  | 是    |
| F04 | 运行状态分析   | 维度切换（按企业/按车型/按行政区域）、里程对比、时长分布、速度区间、数据采集质量                                             | P0  | 是    |
| F05 | 预警事件分析   | 单页面架构，聚焦 F15 预警类 3 类事件（车辆运行异常/碰撞事件/车载终端异常），含 6 统计卡片、4 图表、明细列表、Leaflet 地图                      | P0  | 是    |
| F06 | 测试里程分析   | 维度切换（单车/企业/区域）、车辆里程列表、企业里程柱状图、区域里程表格、里程增长趋势                                           | P0  | 是    |
| F07 | 产业发展分析   | 核心指标卡片、企业规模增长趋势、测试规模趋势、场景渗透率、企业贡献排名                                                   | P0  | 是    |
| F08 | 企业数据查询   | Tab 切换（企业测试查询/企业脱手率查询），按日粒度数据，支持筛选、分页、勾选导出                                                      | P0  | 是    |
| F09 | 安全员数据查询  | 安全员干预记录查询，按日粒度数据，支持按企业、姓名、日期范围筛选、分页、勾选导出                                                         | P0  | 是    |
| F10 | 车辆数据查询   | 5Tab 切换（牌照查询/测试查询/脱手率查询/实时数据查询/作业数据查询），支持筛选、分页、勾选导出                                      | P0  | 是    |
| F11 | 设备数据查询   | T-Box 设备信息查询，支持按设备 ID、在线状态、SIM 卡号等筛选、分页、导出                                               | P0  | 是    |
| F12 | 车辆故障信息查询 | 车辆故障记录查询，支持按车牌号、VIN、故障码、故障状态筛选、分页、导出                                                  | P0  | 是    |
| F13 | 事故数据解析   | 事故报告上传、OCR 解析、解析结果列表、OCR 详情查看、人工校正                                                      | P0  | 是    |
| F14 | 事故成因分析   | 3Tab 架构（片区事故成因分析/路段事故成因分析/单起事故成因分析），含统计卡片、图表、明细表                                       | P0  | 是    |
| F15 | 事故特征分析   | 4Tab 架构（事故时空特征/事故关联检索/多维特征画像/碰撞形态分析），含热力图、标签云、交叉矩阵                                     | P0  | 是    |
| F16 | 事故分析研判   | 5Tab 架构（多发地点分析/风险区域分析/事故趋势分析/车辆主体分析/分析报告与管控建议）                                         | P0  | 是    |
| F17 | 道路测试专题分析 | 3Tab 架构（总体监测/企业监测/车辆运行分析），含指标卡片、图表、企业对比                                                | P0  | 是    |
| F18 | 示范应用专题分析 | 3Tab 架构（总体监测/企业监测/车辆运行分析），含服务次数、示范场景分析                                                 | P0  | 是    |
| F19 | 示范运营专题分析 | 4Tab 架构（总体监测/企业监测/车辆运行分析/示范运营监测），含营收指标、收费分析                                            | P0  | 是    |

**说明：** 本期建设覆盖监测数据分析模块全部 19 个功能点，其中查询类功能（F08-F12）均支持分页和导出，分析类功能（F01-F07、F13-F19）均支持图表展示和筛选。

---

## 3. 功能详细设计

### 3.1 F01 测试数据总览

#### 3.1.1 页面布局

页面自上而下分为 6 个区域：

1. **时间筛选栏**：快捷时间按钮 + 自定义起止日期选择器 + 查询/刷新按钮
2. **累计统计卡片区**：单行 4 个卡片（测试企业数/临牌发放数/测试时长/下发提醒）
3. **图表区**：两列布局（测试主体累计测试里程横向柱状图 + 测试里程趋势分析组合图）
4. **当日测试情况区**：6 个紧凑卡片 + 手动刷新按钮
5. **当前测试企业车辆统计表**：企业名称/当日测试车辆数/正在测试车辆数
6. **实时车辆动态列表**：5 条/页分页列表 + 8 秒自动轮换刷新

#### 3.1.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| 快捷时间 | 按钮组 | 枚举值：今日、昨日、最近 7 天（默认选中）、最近 30 天、本月 |
| 开始时间 | 日期选择器 | 联动快捷时间自动填充 |
| 结束时间 | 日期选择器 | 联动快捷时间自动填充 |
| 查询按钮 | 主按钮 | 触发全部卡片和图表数据刷新 |
| 刷新按钮 | 次按钮 | 刷新累计数据，带旋转图标 |

**查询条件显示规则：** 筛选条件一行展示，下方辅助文字说明 " 以下累计数据和图表共用时间筛选 "。

#### 3.1.3 图表区

| 图表名称 | 图表类型 | 数据维度 | 说明 |
| ---- | ---- | ---- | ---- |
| 测试主体累计测试里程 | 横向柱状图 | X 轴为里程 (km)，Y 轴为企业名称 | 所选周期内各企业累计里程，柱体右侧显示数值 |
| 测试里程趋势分析 | 组合图（折线 + 面积 + 虚线） | 自动驾驶里程（绿色面积折线）、人工驾驶里程（橙色折线）、自动驾驶占比（蓝色虚线，副坐标轴） | 时间粒度按筛选范围适配：今日/昨日按时段，最近 7 天按日，最近 30 天/本月按周 |

#### 3.1.4 当日测试情况卡片

| 卡片标题 | 说明 |
| ---- | ---- |
| 当前测试企业数 | 今日有测试活动的企业数 |
| 测试车辆数 | 今日参与测试车辆总数 |
| 当日测试里程 (km) | 今日累计测试里程 |
| 当日测试时长 (小时) | 今日累计测试时长 |
| 载人测试总里程 (km) | 载人测试累计里程 |
| 载人测试总时长 (小时) | 载人测试累计时长 |

- 带手动刷新按钮（图标 + 点击触发），点击可模拟实时数据波动

#### 3.1.5 数据列表区

**当前测试企业车辆统计表：**

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 企业名称 | company_name | auto | |
| 2 | 当日测试车辆数 | test_vehicles | 150px | 该企业当日测试车辆总数 |
| 3 | 正在测试车辆数 | active_vehicles | 150px | 自动驾驶/人工驾驶且车速>0 的车辆数，绿色标签展示 |

**实时车辆动态列表：**

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 临时号牌 | plate | 140px | 蓝色高亮显示 |
| 2 | 所属企业 | enterprise | 120px | |
| 3 | 驾驶模式 | mode | 100px | 自动驾驶（绿色标签）、人工驾驶（黄色标签）、远程驾驶（蓝色标签） |
| 4 | 车速 (km/h) | speed | 80px | |
| 5 | 最后上报时间 | time | 160px | |

**排序规则：** 按最后上报时间倒序排列。

**分页规则：** 默认每页 5 条，见附录 A.1（特殊场景：每页 5 条）。

**特殊设计：** 8 秒自动轮换刷新机制，模拟新上报数据，带数据总数显示。

#### 3.1.6 交互流程
1. 页面加载 → 默认选中 " 最近 7 天 " → 渲染全部卡片、图表、表格、实时列表
2. 用户点击快捷时间按钮 → 更新筛选日期 + 刷新累计数据和图表 + 按钮高亮切换
3. 用户自定义日期后点击 " 查询 " → 刷新全部数据 + Toast 提示 " 数据已更新 "
4. 用户点击 " 刷新 " → 刷新累计数据 + Toast 提示 " 累计数据已刷新 "
5. 用户点击当日测试情况刷新图标 → 模拟数据波动 + 刷新当日卡片和统计表 + Toast 提示 " 实时数据已刷新 "
6. 实时车辆动态列表每 8 秒自动刷新 → 更新上报时间和车速（模拟数据轮换）

---

### 3.2 F02 异常告警统计

> **原始需求**：对异常状态事件模块（F15）中 " 告警类 " 事件（临牌有效期、电子围栏越界、测试时段合规性、安全员到岗状态）进行分类汇聚与统计分析，对不同类型告警事件的发生频次、企业分布、处理情况进行结构化展示，实现对测试运行过程中合规性违规事件的集中监测与风险态势感知。

> **V1.3 变更说明**：取消 " 设备异常/行为异常 " 分类维度和 " 告警级别 " 维度；告警类型由 6 种调整为 4 种（对齐 F15 告警类）；页面由 3Tab 架构简化为单页面；统计卡片 8→6；图表区新增企业告警排名 TOP10 和企业告警类型分布。

#### 3.2.1 页面布局

页面为单页面，自上而下分为 5 个区域：

1. **筛选栏**：时间范围 + 告警类型 + 所属企业 + 车牌号 + 查询/导出按钮
2. **统计卡片区**：6 个卡片（当日告警总数/临牌有效期告警/电子围栏越界告警/测试时段合规告警/已处理告警/告警处理率）
3. **图表区**：3 行 6 个图表（告警类型分布环形图/告警趋势面积折线图/告警类型趋势对比多折线图/告警热点地图/企业告警数排名 TOP10 横向柱状图/企业告警类型分布堆叠柱状图）
4. **告警明细列表**：10 列列表 + 分页（默认 10 条/页）+ 详情弹窗

#### 3.2.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| 开始日期 | 日期选择器 | 默认最近 7 天 |
| 结束日期 | 日期选择器 | 默认当日 |
| 告警类型 | 下拉单选 | 枚举值：全部、临牌有效期告警、电子围栏越界告警、测试时段合规性告警、安全员到岗状态告警（对齐 F15 告警类） |
| 所属企业 | 下拉单选 | 枚举值：全部 + 8 家示例企业 |
| 车牌号 | 文本输入框 | 支持模糊匹配 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 刷新全部卡片、图表和列表 | 全部 |
| 导出 | 导出当前筛选条件下的告警数据（见附录 A.3） | 全部 |

#### 3.2.3 统计卡片配置

| 序号 | 卡片标题 | 颜色标识 | 说明 |
| ---- | -------- | -------- | ---- |
| 1 | 当日告警总数 | 红色 (#ff4d4f) | 当日全部告警数量 |
| 2 | 临牌有效期告警 | 蓝色 (#1677ff) | 临牌有效期告警数量 |
| 3 | 电子围栏越界告警 | 橙色 (#fa8c16) | 电子围栏越界告警数量 |
| 4 | 测试时段合规告警 | 紫色 (#722ed1) | 测试时段合规性告警数量 |
| 5 | 已处理告警 | 绿色 (#52c41a) | 处理状态为 " 已处理 " 的告警数量 |
| 6 | 告警处理率 | 青色 (#13c2c2) | (已处理告警数/告警总数)×100% |

#### 3.2.4 图表区

**第一行（2 列）：**

| 图表名称 | 图表类型 | 数据说明 |
| ---- | ---- | -------- |
| 告警类型分布 | 环形图（饼图） | 展示 4 类告警的占比分布 |
| 告警趋势（近 30 天） | 面积折线图 | 告警总数随时间变化，含平均值虚线标记 |

**第二行（2 列）：**

| 图表名称 | 图表类型 | 数据说明 |
| ---- | ---- | -------- |
| 告警类型趋势对比 | 多折线图 | 4 类告警分别随时间变化的趋势对比 |
| 告警热点地图 | Leaflet 地图 | 随机生成 60 个点位，按 4 类告警颜色着色（临牌蓝/围栏橙/时段紫/安全员青），支持点击弹窗 |

**第三行（2 列）：**

| 图表名称 | 图表类型 | 数据说明 |
| ---- | ---- | -------- |
| 企业告警数排名 TOP10 | 横向柱状图 | 按企业告警数降序，>=60 标红色，>=40 标橙色，其余蓝色 |
| 企业告警类型分布 | 堆叠柱状图 | 各企业 4 类告警的构成堆叠展示 |

#### 3.2.5 数据列表区（告警明细列表）

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 告警编号 | id | 160px | ALT04~ALT07- 日期 - 序号格式（对齐 F15） |
| 2 | 时间 | time | 140px | |
| 3 | 车牌号 | plate | 90px | |
| 4 | VIN 码 | vin | 130px | 17 位编码，等宽字体 |
| 5 | 所属企业 | enterprise | 110px | |
| 6 | 告警类型 | type | 140px | 4 类告警，按类型颜色标签 |
| 7 | 地点 | location | 160px | 10 个预设路段点位 |
| 8 | 持续时长 | duration | 90px | 如 "2h 15min"，未结束显示 " 进行中 " |
| 9 | 处理状态 | status | 90px | 待处理 (橙色)/处理中 (蓝色)/已处理 (绿色) |
| 10 | 操作 | action | 70px | " 详情 " 按钮 |

**排序规则：** 默认按告警时间倒序。

**分页规则：** 默认每页 10 条，支持切换 10/20/50/100 条（见附录 A.1）。

**操作列按钮：**

| 操作 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 详情 | 弹出告警详情弹窗 | 全部 |

#### 3.2.6 弹窗规格（告警详情弹窗）

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px |
| 最大宽度 | 90vw |
| 位置 | 居中弹出 |
| 遮罩 | 半透明 |
| 标题 | " 告警详情 " |
| 内容布局 | Ant Design 描述列表布局（两列） |

**详情弹窗展示字段：**

- 基本信息：告警编号、告警时间、告警类型（颜色标签）、处理状态、车辆号牌、VIN 码、所属企业、告警地点、持续时长、数据来源、告警详情

#### 3.2.7 交互流程
1. 页面加载 → 默认 " 最近 7 天 " → 渲染统计卡片、图表、地图、告警列表
2. 用户选择筛选条件后点 " 查询 " → 刷新全部面板数据 + Toast 提示
3. 告警列表点击 " 详情 " → 弹出详情弹窗 → 展示完整告警信息（描述列表布局）
4. 点击弹窗关闭按钮/遮罩 → 关闭弹窗

---
### 3.3 F03 违法事故统计

#### 3.3.1 页面布局

页面自上而下分为 5 个区域：

1. **筛选栏**：时间范围 + 行政区域 + 事故类型 + 严重程度 + 查询/导出按钮
2. **统计卡片区**：4 个卡片（事故总数/已上报/已提交交通事故报告/已提交分析报告）
3. **图表区第一行**：两列（事故类型分布饼图 + 行政区域事故分布柱状图）
4. **事故趋势图**：折线图展示近 30 天事故趋势
5. **事故列表**：10 列列表 + 分页（默认 10 条/页）+ 详情弹窗

#### 3.3.2 查询筛选区

| 元素   | 类型    | 配置说明                 |
| ---- | ----- | -------------------- |
| 开始日期 | 日期选择器 | 默认 2026-04-01         |
| 结束日期 | 日期选择器 | 默认 2026-05-08         |
| 行政区域 | 下拉单选  | 枚举值：关联襄阳市行政区划字典      |
| 事故类型 | 下拉单选  | 枚举值：全部、碰撞、刮擦、追尾、行人冲突 |
| 严重程度 | 下拉单选  | 枚举值：全部、死亡、重伤、轻伤、无伤亡  |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 刷新列表、图表、统计卡片 | 全部 |
| 导出 Excel | 导出当前筛选结果（见附录 A.3） | 全部 |

#### 3.3.3 数据列表区

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 事故编号 | id | 120px | ACC- 年份 - 序号格式，加粗 |
| 2 | 时间 | time | 140px | |
| 3 | 地点 | location | 160px | 省略号截断 |
| 4 | 事故类型 | accident_type | 90px | 碰撞/追尾 (红色标签)、行人冲突 (黄色标签)、刮擦 (蓝色标签) |
| 5 | 严重程度 | severity | 90px | 死亡 (红色)、重伤 (黄色)、轻伤 (蓝色)、无伤亡 (绿色) |
| 6 | 车牌号 | plate | 100px | |
| 7 | VIN 码 | vin | 120px | 等宽字体 |
| 8 | 所属企业 | company | 100px | |
| 9 | 事故分类 | category | 130px | 已上报 (黄色)/已提交交通事故报告 (蓝色)/已提交分析报告 (绿色) |
| 10 | 操作 | action | 80px | " 详情 " 按钮 |

**行交互规则：** 行 hover 高亮，点击行打开详情弹窗。

**分页规则：** 默认每页 10 条，支持切换 10/20/50 条。

#### 3.3.4 弹窗规格（事故详情弹窗）

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px |
| 最大宽度 | 90vw |
| 位置 | 居中弹出 |
| 遮罩 | 半透明，点击遮罩关闭 |
| 标题 | " 事故详情 - {事故编号}" |

**详情内容：** 事故编号、发生时间、地点、事故类型、严重程度、车牌号、VIN 码、所属企业、事故分类、伤亡情况、事故描述 + 关联信息（同车辆历史事故/同路段历史事故/同企业事故数）

#### 3.3.5 交互流程
1. 页面加载 → 默认筛选条件 → 渲染统计卡片、图表、列表
2. 用户修改筛选条件后点 " 查询 " → 重新筛选 + 更新统计卡片 + 更新图表 + 列表回到第 1 页
3. 点击行/详情按钮 → 弹出事故详情弹窗
4. 点击弹窗遮罩/关闭按钮 → 关闭弹窗
5. 点击 " 导出 Excel" → Toast 提示 " 导出成功 "

---

### 3.4 F04 运行状态分析

#### 3.4.1 页面布局

页面自上而下分为 6 个区域：

1. **页面头部**：标题 + 导出分析报告按钮
2. **筛选栏**：时间范围 + 所属企业 + 行政区域 + 运行模式 + 查询/重置按钮
3. **维度切换 Tab**：按企业/按车型/按行政区域（带激活态蓝色背景）
4. **统计卡片区**：5 个卡片（总测试里程/总测试时长/平均速度/数据完整率/在线车辆数）
5. **图表区第一行**：两列（各维度测试里程对比柱状图 + 各维度测试时长分布饼图）
6. **图表区第二行**：两列（速度区间分布柱状图 + 数据完整率趋势折线图）
7. **数据采集质量表格**：10 列列表 + 分页 + 详情弹窗

#### 3.4.2 查询筛选区

| 元素 | 类型 | 配置说明                  |
| ---- | ---- | -------- |
| 开始日期 | 日期选择器 | 默认 2026-04-01          |
| 结束日期 | 日期选择器 | 默认 2026-05-08          |
| 所属企业 | 下拉单选 | 枚举值：全部 + 4 家示例企业       |
| 行政区域 | 下拉单选 | 枚举值：关联襄阳市行政区划字典       |
| 运行模式 | 下拉单选 | 枚举值：全部、自动驾驶、人工驾驶、远程接管 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 根据筛选条件重新加载数据 | 全部 |
| 重置 | 恢复筛选条件默认值 | 全部 |
| 导出分析报告 | 弹出导出报告弹窗（选择模板和格式） | 全部 |

#### 3.4.3 数据列表区（数据采集质量明细）

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 车牌号 | plate | 100px | 加粗 |
| 2 | VIN 码 | vin | 120px | 等宽字体 |
| 3 | 所属企业 | company | 100px | |
| 4 | 最后上报时间 | last_report_time | 140px | |
| 5 | 数据完整率 | completeness | 100px | ≥95% 绿色、≥90% 蓝色、<90% 红色标签 |
| 6 | 缺失字段 | missing_fields | 80px | X 个 |
| 7 | 数据延迟 | data_delay | 90px | ≤50ms 绿色、≤120ms 黄色、>120ms 红色 |
| 8 | 当日里程 | daily_mileage | 90px | X km |
| 9 | 运行模式 | mode | 90px | 自动驾驶 (蓝色)/人工驾驶 (绿色)/远程接管 (黄色) |
| 10 | 操作 | action | 80px | " 详情 " 按钮 |

**分页规则：** 默认每页 10 条，支持切换 10/20/50/100 条，显示 " 第 X/Y 页 "、上一页/下一页按钮。

#### 3.4.4 弹窗规格

**导出报告弹窗：**

| 属性 | 值 |
| ---- | -- |
| 宽度 | 600px |
| 位置 | 居中弹出 |
| 内容 | 选择报告模板（下拉）、导出文件格式（PDF/Word 单选） |
| 按钮 | 取消 + 确认导出 |

**车辆运行详情弹窗：**

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px |
| 最大宽度 | 90vw |
| 最大高度 | 80vh |
| 位置 | 居中弹出 |
| 内容 | 车牌号、VIN 码、所属企业、车型、行政区域、最后上报时间、数据完整率、缺失字段数、数据延迟、当日里程、运行模式 + 近 7 日数据质量趋势折线图 |

#### 3.4.5 交互流程
1. 页面加载 → 默认 " 按企业 " 维度 → 渲染统计卡片、图表、质量表格
2. 用户点击维度切换 Tab → 更新图表标题和数据 + 统计卡片数据联动变化
3. 用户点击 " 详情 "/行 → 弹出车辆运行详情弹窗，含近 7 日趋势图
4. 用户点击 " 导出分析报告 " → 弹出导出弹窗 → 选择模板和格式 → 确认导出 → Toast 提示 " 分析报告已生成 "
5. 点击 " 重置 " → 恢复筛选条件默认值

---

### 3.5 F05 预警事件分析

> **原始需求**：对异常状态事件模块（F15）中 " 预警类 " 事件（车辆运行异常、车辆碰撞事件、车载终端异常）进行分类汇聚与统计分析，对不同类型预警事件的发生频次、处理情况进行结构化展示，实现对测试运行过程中安全/技术风险的早期发现与态势感知。

> **V1.3 变更说明**：废弃原 5 种 ADAS 预警类型（碰撞预警、车道偏离预警、行人碰撞预警、交通标识预警、前车碰撞预警），改为对齐 F15 预警类的 3 种类型；统计卡片 4→6；图表区 3→4（新增预警类型趋势对比多折线图）；编号格式改为 EW01~EW03- 日期 - 序号。

#### 3.5.1 页面布局

页面为单页面，自上而下分为 5 个区域：

1. **筛选栏**：时间范围 + 预警类型 + 所属企业 + 车牌号 + 查询/导出按钮
2. **统计卡片区**：6 个卡片（当日预警总数/车辆运行异常预警/车辆碰撞事件预警/车载终端异常预警/已处理预警/影响车辆数）
3. **图表区**：2 行 4 个图表（预警类型分布环形图/预警趋势面积折线图/预警类型趋势对比多折线图/预警热点地图）
4. **预警明细列表**：9 列列表 + 分页（默认 10 条/页）+ 详情弹窗

#### 3.5.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| 开始日期 | 日期选择器 | 默认最近 7 天 |
| 结束日期 | 日期选择器 | 默认当日 |
| 预警类型 | 下拉单选 | 枚举值：全部、车辆运行异常预警、车辆碰撞事件预警、车载终端异常预警（对齐 F15 预警类） |
| 所属企业 | 下拉单选 | 枚举值：全部 + 8 家示例企业 |
| 车牌号 | 文本输入框 | 支持模糊匹配 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 刷新全部卡片、图表和列表 | 全部 |
| 导出 | 导出当前筛选条件下的预警数据（见附录 A.3） | 全部 |

#### 3.5.3 统计卡片配置

| 序号 | 卡片标题 | 颜色标识 | 说明 |
| ---- | -------- | -------- | ---- |
| 1 | 当日预警总数 | 红色 (#ff4d4f) | 当日全部预警数量 |
| 2 | 车辆运行异常预警 | 蓝色 (#1677ff) | 车辆运行异常预警数量 |
| 3 | 车辆碰撞事件预警 | 橙色 (#fa8c16) | 车辆碰撞事件预警数量 |
| 4 | 车载终端异常预警 | 紫色 (#722ed1) | 车载终端异常预警数量 |
| 5 | 已处理预警 | 绿色 (#52c41a) | 处理状态为 " 已处理 " 的预警数量 |
| 6 | 影响车辆数 | 青色 (#13c2c2) | 当日预警涉及的不同车辆数量 |

#### 3.5.4 图表区

**第一行（2 列）：**

| 图表名称 | 图表类型 | 数据说明 |
| ---- | ---- | -------- |
| 预警类型分布 | 环形图（饼图） | 展示 3 类预警的占比分布 |
| 预警趋势（近 30 天） | 面积折线图 | 预警总数随时间变化，含平均值虚线标记 |

**第二行（2 列）：**

| 图表名称 | 图表类型 | 数据说明 |
| ---- | ---- | -------- |
| 预警类型趋势对比 | 多折线图 | 3 类预警分别随时间变化的趋势对比 |
| 预警热点地图 | Leaflet 地图 | 随机生成 60 个点位，按 3 类预警颜色着色（运行异常蓝/碰撞事件橙/终端异常紫），支持点击弹窗 |

#### 3.5.5 数据列表区（预警明细列表）

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 预警编号 | id | 160px | EW01~EW03- 日期 - 序号格式（对齐 F15） |
| 2 | 时间 | time | 140px | |
| 3 | 预警类型 | type | 150px | 3 类预警，按类型颜色标签 |
| 4 | 车牌号 | plate | 90px | |
| 5 | VIN 码 | vin | 130px | 17 位编码，等宽字体 |
| 6 | 所属企业 | enterprise | 110px | |
| 7 | 地点 | location | 160px | 10 个预设路段点位 |
| 8 | 处理状态 | status | 90px | 待处理 (橙色)/处理中 (蓝色)/已处理 (绿色) |
| 9 | 操作 | action | 70px | " 详情 " 按钮 |

**排序规则：** 默认按预警时间倒序。

**分页规则：** 默认每页 10 条，支持切换 10/20/50/100 条（见附录 A.1）。

**操作列按钮：**

| 操作 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 详情 | 弹出预警详情弹窗 | 全部 |

#### 3.5.6 弹窗规格（预警详情弹窗）

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px |
| 最大宽度 | 90vw |
| 位置 | 居中弹出 |
| 遮罩 | 半透明 |
| 标题 | " 预警详情 " |
| 内容布局 | Ant Design 描述列表布局（两列） |

**详情弹窗展示字段：**

- 基本信息：预警编号、预警时间、预警类型（颜色标签）、处理状态、车辆号牌、VIN 码、所属企业、预警地点、数据来源、关联事故、预警详情

#### 3.5.7 交互流程
1. 页面加载 → 默认 " 最近 7 天 " → 渲染统计卡片、图表、地图、预警列表
2. 用户选择筛选条件后点 " 查询 " → 刷新全部面板数据 + Toast 提示
3. 预警列表点击 " 详情 " → 弹出详情弹窗 → 展示完整预警信息
4. 点击弹窗关闭按钮/遮罩 → 关闭弹窗

#### 3.5.5 交互流程
1. 页面加载 → 默认筛选条件 → 渲染统计卡片、图表、地图、列表
2. 用户修改筛选条件后点 " 查询 " → 重新筛选 + 更新统计卡片 + 更新图表 + 列表回第 1 页
3. 点击行/详情按钮 → 弹出事件详情弹窗
4. 点击弹窗遮罩/关闭按钮 → 关闭弹窗

---

### 3.6 F06 测试里程分析

#### 3.6.1 页面布局

页面自上而下分为 5 个区域：

1. **筛选栏**：时间范围 + 运行模式 + 查询按钮
2. **统计卡片区**：4 个卡片（总测试里程/总测试时长/测试车辆数/自动驾驶里程占比）
3. **维度切换 Tab**：单车/企业/区域（带激活态蓝色背景）
4. **维度内容区**：
   - 单车维度：车辆里程列表（11 列，支持排序）+ 分页
   - 企业维度：企业总里程横向柱状图（支持点击下钻）
   - 区域维度：区域里程统计表格（行政区域/里程/占比/车辆数/里程密度）
5. **里程增长趋势图**：按维度展示月度里程增量趋势（折线图）

#### 3.6.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| 时间范围（起） | 日期选择器 | 默认 2026-01-01 |
| 时间范围（止） | 日期选择器 | 默认 2026-05-08 |
| 运行模式 | 下拉单选 | 枚举值：全部、自动驾驶、人工驾驶 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 刷新当前维度数据 | 全部 |

#### 3.6.3 数据列表区（单车维度）

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 车牌号 | plate | 100px | 加粗 |
| 2 | VIN 码 | vin | 120px | 等宽字体 |
| 3 | 所属企业 | company | 100px | |
| 4 | 总测试里程 (km) | total_mileage | 110px | 千分位格式 |
| 5 | 自动驾驶里程 (km) | ad_mileage | 120px | 千分位格式 |
| 6 | 人工驾驶里程 (km) | manual_mileage | 120px | 千分位格式 |
| 7 | 测试时长 (h) | duration | 100px | 千分位格式 |
| 8 | 平均速度 (km/h) | avg_speed | 100px | |
| 9 | 自动驾驶占比 | auto_ratio | 100px | ≥80% 绿色、≥60% 蓝色、<60% 黄色标签 |
| 10 | 最近测试日期 | last_test_date | 120px | |
| 11 | 操作 | action | 80px | " 详情 " 按钮 |

**排序规则：** 支持点击表头排序（默认按测试里程降序），排序图标显示排序方向。

**分页规则：** 默认每页 10 条，支持切换 10/20/50 条。

#### 3.6.4 弹窗规格

**车辆详情弹窗：**

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px |
| 位置 | 居中弹出 |
| 内容 | 车牌号、VIN 码、所属企业、总测试里程、自动驾驶里程、人工驾驶里程、测试时长、平均速度、自动驾驶占比、最近测试日期 |

**企业下钻弹窗（企业维度点击）：**

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px |
| 位置 | 居中弹出 |
| 内容 | 企业名称 + 车辆数 + 总里程汇总 + 车辆列表（仅展示前 10 条） |

#### 3.6.5 交互流程
1. 页面加载 → 默认 " 单车 " 维度 → 渲染车辆里程列表 + 里程增长趋势图
2. 用户点击表头 → 排序切换（升序/降序），排序图标变色
3. 用户切换到 " 企业 " 维度 → 显示企业列程柱状图 + 趋势图
4. 用户在企业柱状图中点击某企业柱体 → 弹出该企业下车辆列表弹窗
5. 用户切换到 " 区域 " 维度 → 显示区域里程统计表 + 趋势图
6. 用户点击 " 详情 " → 弹出车辆详情弹窗

---

### 3.7 F07 产业发展分析

#### 3.7.1 页面布局

页面自上而下分为 6 个区域：

1. **页面头部**：标题 + 导出报告按钮
2. **筛选栏**：时间范围 + 行政区域 + 企业类型 + 查询按钮
3. **核心指标卡片区**：5 个卡片（累计接入企业数/累计测试车辆数/累计测试里程/场景覆盖率/本月新增企业数）
4. **第一行图表**：两列（企业规模增长趋势组合图 + 测试规模趋势组合图），均支持展开/收起数据表
5. **第二行图表**：两列（场景渗透率堆叠柱状图 + 各场景车辆数占比饼图）+ 场景渗透率明细表
6. **第三行图表**：两列（测试主体贡献排名 Top10 横向柱状图 + 贡献排名明细表）

#### 3.7.2 查询筛选区

| 元素 | 类型 | 配置说明                  |
| ------- | ----- | --------------------- |
| 时间范围（起） | 日期选择器 | 默认 2026-01-01          |
| 时间范围（止） | 日期选择器 | 默认 2026-12-31          |
| 行政区域 | 下拉单选 | 枚举值：关联襄阳市行政区划字典       |
| 企业类型 | 下拉单选 | 枚举值：全部、整车企业、科技公司、出行平台 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 根据筛选条件重新加载数据 | 全部 |
| 导出报告 | 弹出导出报告弹窗（见附录 A.8） | 全部 |

#### 3.7.3 场景渗透率明细表

| 序号 | 字段名 | 字段标识 | 说明 |
| ---- | ------ | -------- | ---- |
| 1 | 场景类型 | scene | 城区道路、园区道路、环卫作业、物流配送、公交运营、出租运营 |
| 2 | L2 车辆数 | l2_count | |
| 3 | L3 车辆数 | l3_count | |
| 4 | L4 车辆数 | l4_count | |
| 5 | 总车辆数 | total | |
| 6 | 占比 | ratio | 百分比 |

#### 3.7.4 贡献排名明细表

| 序号 | 字段名 | 字段标识 | 说明 |
| ---- | ------ | -------- | ---- |
| 1 | 排名 | rank | 加粗 |
| 2 | 企业名称 | name | |
| 3 | 测试里程 (km) | mileage | 千分位格式 |
| 4 | 里程占比 | ratio | |
| 5 | 投入车辆数 | vehicles | |
| 6 | 主要测试场景 | scene | |

#### 3.7.5 交互流程
1. 页面加载 → 渲染全部指标卡片、图表、明细表
2. 用户点击 " 展开数据表 " → 显示图表下方对应的数据表格
3. 用户点击 " 收起数据表 " → 隐藏数据表格
4. 用户点击 " 导出报告 " → 弹出导出弹窗 → 选择模板和格式 → 确认导出

---

### 3.8 F08 企业数据查询

#### 3.8.1 页面布局

页面顶部为 Tab 切换（企业测试查询/企业脱手率查询），下方为对应 Tab 内容。

#### 3.8.2 查询筛选区（企业测试查询）

| 元素   | 类型     | 配置说明                              |
| ---- | ------ | --------------------------------- |
| 企业名称 | 文本输入框  | 支持模糊搜索                            |
| 日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-01-01 至 2026-04-30；开始日期不得晚于结束日期 |

**操作按钮：**

| 按钮 | 触发行为                   | 可见角色 |
| -- | ---------------------- | ---- |
| 查询 | 校验日期范围通过后刷新列表        | 全部   |
| 重置 | 恢复筛选条件默认值并清空勾选状态      | 全部   |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部   |

#### 3.8.3 数据列表区（企业测试查询）

数据粒度为按日统计，每行为某企业某一日的测试数据汇总。

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 复选框 | row_checkbox | 40px | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | 企业名称 | company_name | 120px | |
| 3 | 日期 | stat_date | 120px | 数据统计日期 |
| 4 | 测试里程 (km) | total_mileage | 120px | 千分位格式 |
| 5 | 自动驾驶测试里程 (km) | ad_mileage | 140px | 千分位格式 |
| 6 | 测试时长 (h) | total_hours | 110px | 千分位格式 |
| 7 | 自动驾驶测试时长 (h) | ad_hours | 140px | 千分位格式 |
| 8 | 示范应用里程 (km) | demo_mileage | 140px | 千分位格式 |
| 9 | 示范应用时长 (h) | demo_hours | 130px | 千分位格式 |

**排序规则：** 默认按日期倒序。

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

#### 3.8.4 查询筛选区（企业脱手率查询）

| 元素   | 类型     | 配置说明                              |
| ---- | ------ | --------------------------------- |
| 企业名称 | 文本输入框  | 支持模糊搜索                            |
| 日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-01-01 至 2026-04-30；开始日期不得晚于结束日期 |

**操作按钮：** 同 3.8.2（查询/重置/导出）。

#### 3.8.5 数据列表区（企业脱手率查询）

数据粒度为按日统计，每行为某企业某一日的脱手数据汇总。

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 复选框 | row_checkbox | 40px | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | 企业名称 | company_name | 120px | |
| 3 | 日期 | stat_date | 120px | 数据统计日期 |
| 4 | 脱手次数 | disengagement_count | 100px | |
| 5 | 自动驾驶测试里程 (km) | ad_mileage | 150px | 千分位格式 |
| 6 | 脱手率 (%) | disengagement_rate | 100px | 脱手率 = 脱手次数 ÷ 自动驾驶测试里程 × 100；分母为 0 时显示 "-" |

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

#### 3.8.6 交互流程
1. 页面加载 → 默认 Tab" 企业测试查询 " → 渲染列表
2. 用户切换 Tab → 切换对应查询列表
3. 用户点击 " 查询 " → 校验日期范围（开始日期晚于结束日期时提示 " 开始日期不能晚于结束日期 "）→ 校验通过刷新列表
4. 用户勾选行复选框 → 表头复选框同步当页选中状态；取消任意一行则表头取消全选
5. 用户点击 " 重置 " → 恢复筛选条件默认值并清空勾选状态
6. 用户点击 " 导出 " → 按附录 A.3：勾选时导出选中行（不支持跨页），未勾选时导出当前筛选全量

---

### 3.9 F09 安全员数据查询

#### 3.9.1 页面布局

页面自上而下分为 2 个区域：

1. **筛选栏**：企业名称 + 安全员姓名 + 日期范围 + 查询/重置/导出按钮
2. **数据列表**：7 列列表（含复选框列）+ 分页

#### 3.9.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 安全员姓名 | 文本输入框 | 支持模糊搜索 |
| 日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-01-01 至 2026-04-30；开始日期不得晚于结束日期 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 校验日期范围通过后刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值并清空勾选状态 | 全部 |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部 |

#### 3.9.3 数据列表区

数据粒度为按日统计，每行为某企业某安全员某一日的干预数据汇总。

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 复选框 | row_checkbox | 40px | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | 企业名称 | company_name | 120px | |
| 3 | 安全员姓名 | safety_driver_name | 100px | |
| 4 | 日期 | stat_date | 120px | 数据统计日期 |
| 5 | 干预次数 | intervention_count | 100px | |
| 6 | 自动驾驶测试里程 (km) | ad_mileage | 150px | 千分位格式 |
| 7 | 干预率 (%) | intervention_rate | 100px | 干预率 = 干预次数 ÷ 自动驾驶测试里程 × 100；分母为 0 时显示 "-" |

**排序规则：** 默认按日期倒序。

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

#### 3.9.4 交互流程
1. 页面加载 → 渲染列表
2. 用户点击 " 查询 " → 校验日期范围（开始日期晚于结束日期时提示 " 开始日期不能晚于结束日期 "）→ 校验通过刷新列表
3. 用户勾选行复选框 → 表头复选框同步当页选中状态；取消任意一行则表头取消全选
4. 用户点击 " 重置 " → 恢复筛选条件默认值并清空勾选状态
5. 用户点击 " 导出 " → 按附录 A.3：勾选时导出选中行（不支持跨页），未勾选时导出当前筛选全量

---

### 3.10 F10 车辆数据查询

#### 3.10.1 页面布局

页面顶部为 5 个 Tab 切换：车辆牌照查询、车辆测试查询、车辆脱手率查询、车辆实时数据查询、车辆作业数据查询。每个 Tab 对应独立的筛选区和列表区。

#### 3.10.2 查询筛选区（车辆牌照查询）

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| 临牌号 | 文本输入框 | 支持模糊搜索 |
| VIN 码 | 文本输入框 | 支持模糊搜索 |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 车辆类型 | 下拉单选 | 枚举值：全部、乘用车、商用车、专用作业车 |
| 牌照状态 | 下拉单选 | 枚举值：全部、有效期内、已过期 |

#### 3.10.3 数据列表区（车辆牌照查询）

| 序号 | 字段名 | 字段标识 | 说明 |
| ---- | ------ | -------- | ---- |
| 1 | 复选框 | row_checkbox | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | 临牌号 | plate | |
| 3 | VIN 码 | vin | |
| 4 | 企业名称 | company_name | |
| 5 | 车辆类型 | vehicle_type | |
| 6 | 牌照状态 | plate_status | 枚举值：有效期内、已过期 |
| 7 | 有效期起止 | plate_valid_range | 开始日期与截止日期组合展示 |
| 8 | 历史牌照 | history_plates | 查看历史牌照记录（牌照号/类型/起止日期/状态） |

**导出规则：** 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量。

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

#### 3.10.4 查询筛选区（车辆测试查询）

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| VIN 码 | 文本输入框 | 支持模糊搜索 |
| 临牌号 | 文本输入框 | 支持模糊搜索 |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-01-01 至 2026-04-30；开始日期不得晚于结束日期 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 校验日期范围通过后刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值并清空勾选状态 | 全部 |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部 |

#### 3.10.5 数据列表区（车辆测试查询）

数据粒度为按日统计，每行为某车辆某一日的测试数据汇总。

| 序号 | 字段名 | 字段标识 | 说明 |
| ---- | ------ | -------- | ---- |
| 1 | 复选框 | row_checkbox | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | VIN 码 | vin | |
| 3 | 临牌号 | plate | |
| 4 | 企业名称 | company_name | |
| 5 | 日期 | stat_date | 数据统计日期 |
| 6 | 测试里程 (km) | total_mileage | 千分位格式 |
| 7 | 自动驾驶测试里程 (km) | ad_mileage | 千分位格式 |
| 8 | 测试时长 (h) | total_hours | 千分位格式 |
| 9 | 自动驾驶测试时长 (h) | ad_hours | 千分位格式 |

**排序规则：** 默认按日期倒序。

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

#### 3.10.6 查询筛选区（车辆实时数据查询）

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| VIN 码 | 文本输入框 | 支持模糊搜索 |
| 临牌号 | 文本输入框 | 支持模糊搜索 |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 时间范围 | 日期时间范围选择 | 开始时间与截止时间组合为单个范围控件，精确到秒；默认 2026-05-01 00:00:00 至 2026-05-08 23:59:59；开始时间不得晚于结束时间 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 校验日期范围通过后刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值并清空勾选状态 | 全部 |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部 |

#### 3.10.7 数据列表区（车辆实时数据查询）

| 序号 | 字段名 | 字段标识 | 说明 |
| ---- | ------ | -------- | ---- |
| 1 | 复选框 | row_checkbox | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | 临牌号 | plate | |
| 3 | VIN 码 | vin | |
| 4 | 所属企业 | company_name | |
| 5 | 数据采集时间 | collect_time | 按数据采集时间倒序 |
| 6 | 经度 | longitude | |
| 7 | 纬度 | latitude | |
| 8 | 车辆驾驶模式 | drive_mode | 枚举值：自动驾驶、人工驾驶 |
| 9 | 车辆速度 | speed | |
| 10 | 档位 | gear | |
| 11 | 操作 | action | 提供查看详情入口 |

**详情字段：**

| 分组 | 字段 |
| --- | --- |
| 基础信息 | 临牌号、VIN码、所属企业、数据采集时间、经度、纬度 |
| 驾驶与车辆动态 | 车辆驾驶模式、接管状态、DMS状态、执行最小风险策略原因、车辆速度、车辆纵向加速度、车辆横向加速度、车辆横摆角速度、车辆侧倾角速度、车辆航向角、失效类型 |
| 车身信号与操控 | 自适应前照明系统状态、近灯光状态、远灯光状态、危险警告信号状态、制动灯状态、左转向信号灯状态、右转向灯状态、车辆喇叭信息、加速踏板开度比例、制动踏板开度比例、制动踏板状态、转向盘角度、档位、雨刮 |
| 远程控制 | 远程控制指令长度、远程控制指令内容 |

**详情交互：** 用户点击“查看”后打开对应运行数据的完整详情；详情支持滚动与全屏查看，关闭后返回原查询结果和勾选状态。

**导出规则：** 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量。

**分页规则：** 每页 20 条。

#### 3.10.8 数据列表区（车辆作业数据查询）

数据粒度为按日统计，每行为某作业车辆某一日的作业数据汇总；列表不设合计行。

| 序号 | 字段名 | 字段标识 | 说明 |
| ---- | ------ | -------- | ---- |
| 1 | 复选框 | row_checkbox | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | VIN 码 | vin | |
| 3 | 临牌号 | plate | |
| 4 | 企业名称 | company_name | |
| 5 | 日期 | stat_date | 数据统计日期 |
| 6 | 作业时长 (h) | work_hours | |
| 7 | 作业里程 (km) | work_mileage | |
| 8 | 油耗 (L) | fuel_consumption | 燃油车辆填值，其他显示 "-" |
| 9 | 电耗 (kWh) | energy_consumption | 电动车辆填值，其他显示 "-" |

**排序规则：** 默认按日期倒序。

**导出规则：** 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量。

#### 3.10.9 查询筛选区（车辆作业数据查询）

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| VIN 码 | 文本输入框 | 支持模糊搜索 |
| 临牌号 | 文本输入框 | 支持模糊搜索 |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-04-01 至 2026-04-30；开始日期不得晚于结束日期 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 校验日期范围通过后刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值并清空勾选状态 | 全部 |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部 |

#### 3.10.10 查询筛选区（车辆脱手率查询）

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| VIN 码 | 文本输入框 | 支持模糊搜索 |
| 临牌号 | 文本输入框 | 支持模糊搜索 |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-01-01 至 2026-04-30；开始日期不得晚于结束日期 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 校验日期范围通过后刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值并清空勾选状态 | 全部 |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部 |

#### 3.10.11 数据列表区（车辆脱手率查询）

数据粒度为按日统计，每行为某车辆某一日的脱手数据汇总。

| 序号 | 字段名 | 字段标识 | 说明 |
| ---- | ------ | -------- | ---- |
| 1 | 复选框 | row_checkbox | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | VIN 码 | vin | |
| 3 | 临牌号 | plate | |
| 4 | 企业名称 | company_name | |
| 5 | 日期 | stat_date | 数据统计日期 |
| 6 | 脱离次数 | disengagement_count | |
| 7 | 自动驾驶里程 (km) | ad_mileage | 千分位格式 |
| 8 | 脱手率 (%) | disengagement_rate | 脱手率 = 脱离次数 ÷ 自动驾驶里程 × 100；分母为 0 时显示 "-" |

**排序规则：** 默认按日期倒序。

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

---

### 3.11 F11 设备数据查询

#### 3.11.1 页面布局

页面自上而下分为 2 个区域：

1. **筛选栏**：VIN 码 + 临牌号 + 企业名称 + 日期范围 + 设备编号 + 在线状态 + SIM 卡号 + 查询/重置/导出按钮
2. **数据列表**：10 列列表（含复选框列）+ 分页

#### 3.11.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| VIN 码 | 文本输入框 | 支持模糊搜索 |
| 临牌号 | 文本输入框 | 支持模糊搜索 |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-04-01 至 2026-05-08；开始日期不得晚于结束日期 |
| 设备编号 | 文本输入框 | 支持模糊搜索 |
| 在线状态 | 下拉单选 | 枚举值：全部、在线、离线 |
| SIM 卡号 | 文本输入框 | 支持模糊搜索 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 校验日期范围通过后刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值并清空勾选状态 | 全部 |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部 |

#### 3.11.3 数据列表区

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 复选框 | row_checkbox | 40px | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | VIN 码 | vin | 160px | |
| 3 | 临牌号 | plate | 100px | |
| 4 | 企业名称 | company_name | 100px | |
| 5 | 设备编号 | device_id | 80px | |
| 6 | 设备版本号 | firmware_version | 100px | |
| 7 | 在线状态 | online_status | 100px | 在线 (绿色标签)/离线 (灰色标签) |
| 8 | 设备硬盘状态 | disk_status | 100px | 正常 (绿色)/存储不足 (黄色)/未知 (灰色) |
| 9 | SIM 卡号 | sim_number | 120px | |
| 10 | 安装日期 | install_date | 120px | |

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

---

### 3.12 F12 车辆故障信息查询

#### 3.12.1 页面布局

页面自上而下分为 2 个区域：

1. **筛选栏**：VIN 码 + 临牌号 + 企业名称 + 发生日期范围 + 故障码 + 故障码状态 + 查询/重置/导出按钮
2. **数据列表**：12 列列表（含复选框列）+ 分页

#### 3.12.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| VIN 码 | 文本输入框 | 支持模糊搜索 |
| 临牌号 | 文本输入框 | 支持模糊搜索 |
| 企业名称 | 文本输入框 | 支持模糊搜索 |
| 发生日期范围 | 日期范围选择 | 开始日期与截止日期组合为单个范围控件；默认 2026-04-01 至 2026-04-30；开始日期不得晚于结束日期 |
| 故障码 | 文本输入框 | 支持模糊搜索 |
| 故障码状态 | 下拉单选 | 枚举值：全部、已消除、未消除 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 校验日期范围通过后刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值并清空勾选状态 | 全部 |
| 导出 | 按附录 A.3：勾选时导出选中行，未勾选时导出当前筛选全量 | 全部 |

#### 3.12.3 数据列表区

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 复选框 | row_checkbox | 40px | 支持单行勾选与表头全选当页，用于勾选导出 |
| 2 | VIN 码 | vin | 130px | 等宽字体 |
| 3 | 临牌号 | plate | 100px | |
| 4 | 企业名称 | company_name | 100px | |
| 5 | 设备编号 | device_id | 80px | |
| 6 | 故障发生时间 | fault_start_time | 140px | |
| 7 | 故障更新时间 | fault_update_time | 140px | |
| 8 | 故障码 | fault_code | 80px | |
| 9 | 故障等级 | fault_level | 80px | 枚举值：1 级、2 级、3 级、4 级（严重程度递增） |
| 10 | 故障描述 | fault_desc | 140px | |
| 11 | 所属系统 | subsystem | 100px | |
| 12 | 故障码状态 | fault_status | 100px | 已消除 (绿色标签)/未消除 (红色标签) |

**分页规则：** 见附录 A.1（查询类列表默认每页 20 条）。

---

### 3.13 F13 事故数据解析

#### 3.13.1 页面布局

页面自上而下分为 3 个区域：

1. **页面头部**：标题 + 上传事故报告按钮
2. **筛选栏**：事故编号 + 涉事车辆 + 事故类型 + 责任认定 + 解析状态 + 查询/重置按钮
3. **事故列表**：9 列列表 + 分页（默认 10 条/页）+ 详情弹窗 + 校正弹窗 + 上传弹窗

#### 3.13.2 查询筛选区

| 元素 | 类型 | 配置说明 |
| ---- | ---- | -------- |
| 事故编号 | 文本输入框 | 支持模糊搜索 |
| 涉事车辆 | 文本输入框 | 车牌号/VIN |
| 事故类型 | 下拉单选 | 枚举值：全部、碰撞、刮擦、追尾、行人冲突、侧翻 |
| 责任认定 | 下拉单选 | 枚举值：全部、全责、主责、同责、次责、无责 |
| 解析状态 | 下拉单选 | 枚举值：全部、已解析、已校正、待解析 |

**操作按钮：**

| 按钮 | 触发行为 | 可见角色 |
| ---- | -------- | -------- |
| 查询 | 刷新列表 | 全部 |
| 重置 | 恢复筛选条件默认值 | 全部 |

#### 3.13.3 数据列表区

| 序号 | 字段名 | 字段标识 | 宽度建议 | 说明 |
| ---- | ------ | -------- | -------- | ---- |
| 1 | 事故编号 | id | 120px | ACC- 年份 - 序号格式，加粗 |
| 2 | 事故时间 | time | 140px | |
| 3 | 事故地点 | location | 180px | 省略号截断 |
| 4 | 涉事车辆 | plate | 100px | |
| 5 | 事故类型 | type | 90px | 碰撞/追尾/侧翻 (红色)、行人冲突 (黄色)、刮擦 (蓝色) |
| 6 | 责任认定 | liability | 90px | 全责/主责 (红色)、同责 (黄色)、次责 (蓝色)、无责 (绿色)、待认定 (灰色) |
| 7 | 伤亡情况 | injury | 90px | |
| 8 | 解析状态 | status | 90px | 已校正 (绿色)/已解析 (蓝色)/待解析 (黄色) |
| 9 | 操作 | action | 140px | 详情 + 校正（已解析时）+ 解析（待解析时） |

**分页规则：** 默认每页 10 条，支持切换 10/20/50 条。

#### 3.13.4 弹窗规格

**详情弹窗（事故解析详情）：**

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px（wide 模式） |
| 位置 | 居中弹出 |
| 内容 | 分组展示：基本信息（编号/时间/地点/类型/责任）、车辆信息（号牌/VIN/企业/自动驾驶等级/驾驶模式）、运行数据（碰撞前速度/最大减速度/转向状态/制动状态/油门开度）、环境信息（天气/光照/路面状况/环境描述）、责任与伤亡（责任认定/伤亡人数/轻伤/重伤/死亡）、附件（报告文件 + 现场照片） |
| 校正信息 | 已校正状态时显示校正人和校正时间 |

**校正弹窗：**

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px（wide 模式） |
| 内容 | 提示信息 + 分组可编辑表单：基本信息（编号/时间/地点/类型）、车辆信息（号牌/VIN/企业/自动驾驶等级/驾驶模式）、运行数据（碰撞前速度/最大减速度/转向状态/制动状态/油门开度）、环境信息（天气/光照/路面状况）、责任与伤亡（责任认定/轻伤/重伤/死亡人数） |
| 按钮 | 取消 + 保存校正 |

**上传弹窗（上传事故处置报告）：**

| 属性 | 值 |
| ---- | -- |
| 宽度 | 800px（wide 模式） |
| 内容 | 流程说明 + 文件拖拽上传区 + 关联车辆选择 + 现场照片上传（最多 5 张）+ 备注 + OCR 模板字段预览 |
| 按钮 | 取消 + 提交并开始解析 |

**上传文件规则：**

| 规则项 | 说明 |
| ---- | ---- |
| 支持格式 | jpg/png/pdf |
| 单文件大小限制 | ≤20MB |
| 多文件上传 | 支持（现场照片最多 5 张） |

#### 3.13.5 交互流程
1. 页面加载 → 渲染全部事故记录列表
2. 用户点击 " 上传事故报告 " → 弹出上传弹窗 → 选择文件 + 关联车辆 → 提交 → Toast 提示 "OCR 解析进行中 " → 2 秒后提示 " 解析完成 "
3. 用户点击 " 详情 " → 弹出解析详情弹窗（分组描述列表布局展示）
4. 用户点击 " 校正 " → 弹出校正弹窗（含编辑表单） → 修改后保存 → Toast 提示 " 校正保存成功 "
5. 待解析记录点击 " 解析 " → Toast 提示 " 正在调用 OCR 服务 " → 1.5 秒后提示 " 解析完成 " → 状态更新
6. OCR 识别模板字段：事故时间、事故地点、涉事车辆号牌、事故类型、责任认定、伤亡情况、天气/路面状况

---

### 3.14 F14 事故成因分析

#### 3.14.1 页面布局

页面顶部为 3 个 Tab 切换：片区事故成因分析、路段事故成因分析、单起事故成因分析。

#### 3.14.2 Tab1- 片区事故成因分析
- **筛选栏**：区域选择 + 时间范围 + 事故类型 + 车辆类型
- **统计卡片**：总事故数、涉及车辆数、主成因类型、高风险片区
- **图表区**：四大主成因占比饼图 + Top10 子因素事故数量柱状图 + 事故点位热力分布图（ECharts 散点图）
- **片区成因详情表**：片区/事故数/主成因类型/Top 子因素/平均贡献权重/操作（查看明细）
- **下钻**：点击 " 查看明细 " 展开该片区事故列表

#### 3.14.3 Tab2- 路段事故成因分析
- **左右分栏布局**：左侧为道路列表（支持搜索），右侧为路段详情
- **路段信息卡片**：道路名称、等级、车道数、长度、RSU 覆盖率、限速值、交叉口密度
- **分析图表**：路段事故成因分布饼图、道路参数与事故关联散点图（支持切换参数）、交通流量与事故对比组合图、事故时段分布热力表

#### 3.14.4 Tab3- 单起事故成因分析
- **事故基本信息卡片**：事故编号、时间、地点、类型、涉事车辆、伤亡情况
- **事故还原时间轴**：以时间线展示关键事件点（含车端与路侧数据），T-30s 至 T+5s
- **各维度贡献度饼图**：展示人、车、路、环境四维度贡献百分比
- **成因详情表格**：维度/子因素/贡献度/判定依据/关键数据（事故值 vs 标准值）
- **关键数据对比表**：列出关键参数的事故值与标准值

#### 3.14.5 交互流程
1. Tab1 页面加载 → 渲染统计卡片、图表、片区表格
2. 点击片区 " 查看明细 " → 展开该片区事故列表
3. Tab2 选择道路 → 更新右侧路段信息卡片和分析图表
4. 搜索道路名称 → 过滤道路列表
5. Tab3 查看单起事故 → 展示时间轴 + 贡献度饼图 + 成因详情表

---

### 3.15 F15 事故特征分析

#### 3.15.1 页面布局

页面顶部为 4 个 Tab 切换：事故时空特征、事故关联检索、多维特征画像、碰撞形态分析。

#### 3.15.2 Tab1- 事故时空特征
- **筛选栏**：时间范围 + 行政区域 + 事故类型
- **图表区**：24 小时分布折线图（支持工作日/周末切换）、星期分布柱状图、月度趋势多年度叠加图、区域分布图、道路等级分布图、事故热力图（Leaflet 热力图层）

#### 3.15.3 Tab2- 事故关联检索
- **高级搜索**：按人（安全员姓名）、车（号牌/VIN/企业）、路（道路名称）、时间（范围）组合查询
- **结果列表**：事故编号/时间/地点/类型/车牌/企业/驾驶员/操作
- **详情查看**：点击行查看全息档案（基本信息、运行数据、成因分析结果、关联的其他事故事件）

#### 3.15.4 Tab3- 多维特征画像
- **交叉统计矩阵**：支持切换 X 轴/Y 轴维度（事故类型×时段等），ECharts 热力图
- **典型特征组合表**：特征组合/事故数/占比/显著性
- **单维度分布图**：支持下拉切换维度
- **特征标签云**：词云图展示高频特征

#### 3.15.5 Tab4- 碰撞形态分析
- **碰撞形态分布饼图**
- **碰撞形态 vs 伤亡等级交叉表**：单元格显示事故数量

#### 3.15.6 交互流程
1. 页面加载 → 默认 Tab1 → 渲染时空特征图表和热力图
2. Tab1 切换工作日/周末 → 刷新 24 小时分布图
3. Tab2 输入高级搜索条件 → 查询 + 渲染结果列表
4. Tab3 切换 X/Y 轴维度 → 更新交叉矩阵图
5. Tab4 查看碰撞形态分布

---

### 3.16 F16 事故分析研判

#### 3.16.1 页面布局

页面顶部为 5 个 Tab 切换：多发地点分析、风险区域分析、事故趋势分析、车辆主体分析、分析报告与管控建议。

#### 3.16.2 Tab1- 多发地点分析
- **筛选栏**：时间范围 + 行政区域 + 事故类型 + 判定阈值
- **GIS 地图**：Leaflet 地图标记多发点（月≥3 起），符号大小按事故数分级
- **多发地点清单**：点位编号/名称/所属区县/道路类型/事故总数/死亡/受伤/主要类型/高发时段/核心致因/操作
- **列表支持分页**

#### 3.16.3 Tab2- 风险区域分析
- **筛选栏**：时间范围 + 行政区域 + 风险等级
- **统计卡片**：高风险区域数/中风险区域数/低风险区域数/安全区域数
- **图表区**：风险区域 GIS 分布（Leaflet）+ 区域风险等级分布图
- **风险区域清单**：区域编号/名称/所属区县/风险等级/事故总数/重伤及以上/主要致因/管控状态/操作
- **管控状态**：管控中 (黄色)、待管控 (蓝色)、已管控 (绿色)

#### 3.16.4 Tab3- 事故趋势分析
- **筛选栏**：时间范围 + 时间粒度 + 分析维度
- **图表区**：实际事故数折线图 + 预测事故数虚线（预测参数由模型自动计算）
- **预测表格**：未来短期（4 周）预测事故数及置信区间

#### 3.16.5 Tab4- 车辆主体分析
- **图表区**：柱状图对比不同品牌/车型的事故率（事故数/测试里程）
- **排序表格**：品牌/车型/事故率（支持排序）

#### 3.16.6 Tab5- 分析报告与管控建议
- **报告生成向导**：
  - 报告类型：周报（上周一至上周日）/ 月报（上个月）/ 自定义（选择起止日期，生成一次性报告。本期不支持定时周期报告）
  - 包含模块（可多选）：总体情况、多发地点、风险区域、趋势分析、管控建议
- **生成流程**：选择配置 → 生成预览 → 用户确认 → 导出 PDF/Word

---

### 3.17 F17 道路测试专题分析

#### 3.17.1 页面布局

页面顶部为 3 个 Tab 切换：总体监测、企业监测、车辆运行分析。

#### 3.17.2 Tab1- 总体监测
- **筛选栏**：时间范围 + 查询/重置按钮
- **指标卡片**：测试企业数/测试车辆数/开放道路里程/当月测试里程
- **趋势图**：月度企业数、车辆数、里程数组合图
- **行政区对比**：柱状图 + 明细表格（行政区/企业数/车辆数/道路里程/当月里程）

#### 3.17.3 Tab2- 企业监测
- **筛选栏**：时间范围 + 企业名称 + 所属区县 + 企业性质 + 查询/重置/导出按钮
- **企业列表**：复选框 + 企业名称/所属区县/企业性质/测试车辆数/累计里程/累计时长/告警数/操作 + 多选对比按钮
- **企业详情弹窗**：点击企业名称进入（含该企业车辆排名、里程趋势等）
- **多选对比弹窗**：雷达图（里程/时长/车辆数/告警率/脱手率）

#### 3.17.4 Tab3- 车辆运行分析
- **车辆列表**：号牌/车型/企业/累计里程/本月里程/接管次数/故障次数
- **车辆详情**（点击车辆进入）：
  - 基本信息（牌号、VIN、尺寸等）
  - 安全员信息（姓名、干预率）
  - 电子围栏越界记录（时间、围栏名称）
  - 里程排名（企业内和全局百分位）
  - 测试场景分析（堆叠柱状图，使用系统预置场景分类）
  - 人工接管分析（接管次数趋势/平均接管里程排名/脱离地点地图/脱离场景分类饼图/脱离原因分类饼图/关键脱离目标分类统计）
  - 车辆异常分析（异常高发时间段和地点，使用系统默认阈值）

---

### 3.18 F18 示范应用专题分析

#### 3.18.1 页面布局

页面顶部为 3 个 Tab 切换：总体监测、企业监测、车辆运行分析。

#### 3.18.2 Tab1- 总体监测
- **筛选栏**：时间范围 + 查询/重置按钮
- **指标卡片**：示范企业数/运营车辆数/覆盖区域数/累计服务次数
- **趋势图**：月度企业数、车辆数、服务次数组合图
- **图表区**：示范场景分布饼图 + 区域覆盖统计表格

#### 3.18.3 Tab2- 企业监测
- **筛选栏**：时间范围 + 企业名称 + 所属区县 + 示范场景 + 查询/重置/导出按钮
- **企业列表**：企业名称/所属区县/运营车辆数/累计里程/运营时长/服务次数/告警数/操作

#### 3.18.4 Tab3- 车辆运行分析
- 类似道路测试车辆分析，增加服务次数、平均载客量、示范场景分析（无人出租、小巴、公交、配送等）

---

### 3.19 F19 商业化试点专题分析

#### 3.19.1 页面布局

页面顶部为 4 个 Tab 切换：总体监测、企业监测、车辆运行分析、示范运营监测。

#### 3.19.2 Tab1- 总体监测
- **指标卡片**：运营企业数/运营车辆数/总运营收入/每公里收入
- **趋势图**：月度企业数、车辆数、运营收入组合图
- **图表区**：运营场景分布饼图 + 营收指标统计表（环比对比）

#### 3.19.3 Tab2- 企业监测
- **企业列表**：企业名称/所属区县/运营车辆数/累计里程/服务次数/运营时长/运营收入/每公里收入/操作

#### 3.19.4 Tab4- 商业化试点监测
- **收费数据**：由运营企业手动上传（Excel 模板），平台自动与服务次数数据进行一致性比对
- **比对结果展示**：匹配一致记录数、缺失收费记录的服务次数、金额偏差记录
- **差异清单表格**：支持导出

---

## 4. 业务规则汇总

### 4.1 计算规则

| 规则名称 | 计算公式 | 说明 |
| ---- | ---- | ---- |
| 脱手率 | (脱手次数 / 自动驾驶里程) × 100 | 分母为 0 时显示 "-" |
| 干预率 | (干预次数 / 自动驾驶里程) × 100 | 分母为 0 时显示 "-" |
| 数据完整率 | (实际上报条数 / 应上报条数) × 100% | |
| 告警处理率 | (已处理告警数 / 告警总数) × 100% | |
| 严重告警占比 | (严重告警数 / 告警总数) × 100% | |
| 影响车辆数 | 告警涉及的不同车辆去重计数 | 按车牌号/VIN 去重 |
| 平均持续时长 | 所有告警持续时长的算术平均值 | 未结束告警按 " 当前时间 - 发生时间 " 计算 |
| 预警→事故转化率 | (关联事故数 / 预警事件总数) × 100% | |
| 自动驾驶占比 | (自动驾驶里程 / 总里程) × 100% | 百分比取 1 位小数 |

### 4.2 编号生成规则

| 规则项 | 告警编号 | 事故编号 | 预警事件编号 |
| ---- | ---- | ---- | ---- |
| 前缀 | ALT | ACC | EVT |
| 日期部分 | YYYYMMDD（取自时间字段） | YYYY | YYYY |
| 序号部分 | 4 位流水号 | 4 位流水号 | 5 位流水号 |
| 示例 | ALT-20260509-0001 | ACC-2026-0001 | EVT-2026-00001 |

### 4.3 时间粒度适配规则

| 筛选范围 | 图表时间粒度 |
| ---- | ---- |
| 今日/昨日 | 按时段（每 4-6 小时） |
| 最近 7 天 | 按日 |
| 最近 30 天 | 按周 |
| 本月 | 按周 |

### 4.4 导出规则

| 规则项 | 说明 |
| ---- | ---- |
| 导出格式 | Excel(.xlsx) |
| 导出上限 | 当前筛选条件下全量结果（上限 10 万条），超限提示缩小范围 |
| 文件命名 | {模块名称}_{导出日期}.xlsx |

### 4.5 权限规则

| 角色 | 数据可见范围 |
| ---- | ---- |
| 政府监管人员 | 全局数据 |
| 企业用户 | 仅本企业相关数据 |
| 系统管理员 | 全局数据 + 审计日志 |

### 4.6 数据刷新规则

| 页面 | 刷新机制 |
| ---- | ---- |
| 实时车辆动态列表 | 8 秒自动轮换刷新 |
| 统计图表 | 手动点击查询/刷新按钮 |

---

## 5. 非功能需求

### 5.1 性能要求

| 指标 | 要求 |
| ---- | ---- |
| 统计图表 API 响应时间 | 95% 分位下≤3 秒 |
| 复杂事故分析 API 响应时间 | ≤10 秒 |
| 列表分页查询响应时间 | ≤2 秒 |
| 导出任务处理 | 异步处理，大数据量导出支持后台任务 |

### 5.2 安全要求

| 指标 | 要求 |
| ---- | ---- |
| 数据权限 | 按角色控制数据可见范围（政府全局，企业仅自身数据） |
| 审计日志 | 报告生成和导出操作记录审计日志 |
| 数据脱敏 | VIN 码等敏感字段按权限控制完整显示 |

### 5.3 兼容性要求

| 指标 | 要求 |
| ---- | ---- |
| 浏览器兼容 | Chrome、Edge 最新两个版本 |
| 分辨率适配 | 1366×768、1920×1080，支持大屏自动填充 |
| 导出兼容 | Excel 2007+ (.xlsx) |

### 5.4 管理端导航与操作入口

1. 管理端按一级业务模块组织访问入口；进入或切换模块后，仅展示该模块下的二级分组与三级功能，直接访问功能页时导航状态须同步到所属模块和当前功能。
2. 管理端不再提供路径式面包屑和独立页面标题；用户通过当前模块、功能菜单高亮及浏览器页面名称识别所处功能。
3. 查询、分析报告导出和事故报告上传等操作的权限、校验、审计和处理结果不变。存在查询条件时，操作入口与当前查询任务关联；存在功能分组时，操作入口与当前分组关联；其他场景保留独立操作入口。

---

## 6. 关联模块说明

| 关联模块 | 关联关系 | 说明 |
| ---- | ---- | ---- |
| 测试示范监管模块 | 数据来源 | 企业接入信息、车辆信息、安全员信息作为分析数据源；事故上报功能触发解析流程 |
| 平台管理模块 | 配置依赖 | 字典配置（区域、车型、告警类型等）；审计日志记录 |
| 车辆监控模块 | 数据共享 | 实时位置、告警数据、运行状态等通过消息队列推送至分析模块 |

---

## 7. 已确认项与待确认项

### 7.1 已确认项

| 编号 | 确认内容 | 确认结果 | 确认日期 |
| ---- | -------- | -------- | -------- |
| C01 | 实时车辆动态列表每页 5 条，8 秒自动轮换 | 已确认 | 2026-05-09 |
| C02 | 异常告警统计 5Tab 架构 | 已确认 | 2026-05-09 |
| C03 | 自定义报告周期为一次性起止日期，本期不支持定时 | 已确认 | 2026-04-30 |
| C04 | F03 违法事故统计图表以 HTML 高保真页面为准（事故类型分布饼图 + 行政区域柱状图 + 事故趋势折线图），F06 单车维度补充自动驾驶/人工驾驶里程及最近测试日期字段 | 已确认 | 2026-06-08 |
| C05 | F02 异常告警统计 Tab 结构调整为 4Tab 架构（告警明细合并入告警概览 Tab），筛选栏以 HTML 页面为准 | 已确认 | 2026-06-08 |
| C06 | F02 回归原始需求核心：3Tab 架构（去掉分析报告 Tab 和预测模型、去掉东风悦享专用分析）；增加设备异常/行为异常融合分类维度；补全影响范围和持续时长指标 | 已确认 | 2026-06-10 |

### 7.2 待确认项

| 编号 | 待确认内容 | 当前处理方案 | 影响范围 | 建议确认方/预计确认时间 |
| ---- | ---------- | ------------ | -------- | ---------------------- |
| Q02 | 事故分析研判中预测参数的计算逻辑 | 当前方案为 " 模型自动计算，不支持用户调整 " | 事故趋势分析 Tab | 算法团队 |
| Q03 | 道路测试 - 车辆详情中异常判定默认阈值（如 30 分钟无移动） | 使用系统默认值，暂不提供用户配置 | 道路测试 - 车辆运行分析 | 业务方 |
| Q04 | 示范运营收费数据 Excel 模板格式 | 待运营企业提供模板参考 | 示范运营 - 运营收费分析 | 运营企业 |
| Q05 | 企业用户的 " 本企业数据 " 边界范围 | 当前按企业名称精确匹配 | 全部查询功能 | 业务方 |
| Q06 | 告警持续时长的数据来源 | 告警系统是否记录告警恢复/关闭时间，若无则按 " 当前时间 - 发生时间 " 计算 | 异常告警统计 | 业务方 |

---

## 8. 验收标准

### 8.1 F01 测试数据总览验收
- [ ] 时间筛选栏 5 个快捷按钮正常工作，默认选中 " 最近 7 天 "
- [ ] 4 个累计统计卡片数据随筛选周期变化
- [ ] 横向柱状图正确展示各企业累计里程
- [ ] 组合图正确展示自动驾驶/人工里程和占比趋势
- [ ] 6 个当日测试情况卡片数据正确，手动刷新按钮可触发数据刷新
- [ ] 企业车辆统计表正确展示企业/当日测试车辆数/正在测试车辆数
- [ ] 实时车辆动态列表每页 5 条，8 秒自动轮换刷新，分页正常

### 8.2 F02 异常告警情况统计验收
- [ ] 单页面正常加载，筛选栏含时间范围/告警类型/所属企业/车牌号
- [ ] 6 个统计卡片数据正确（当日告警总数/临牌有效期/电子围栏越界/测试时段合规/已处理/处理率）
- [ ] 告警类型枚举为 4 类（临牌有效期/电子围栏越界/测试时段合规/安全员到岗），无 " 设备异常/行为异常 " 大类
- [ ] 环形图展示 4 类告警类型分布
- [ ] 面积折线图展示告警趋势，含平均值虚线标记
- [ ] 多折线图展示 4 类告警类型趋势对比
- [ ] Leaflet 热点地图正常加载 60 个随机点位，按 4 类告警颜色着色
- [ ] 企业告警数排名 TOP10 横向柱状图按告警数降序展示
- [ ] 企业告警类型分布堆叠柱状图展示各企业 4 类告警构成
- [ ] 告警明细列表 10 列展示完整（无告警大类、告警级别列），默认每页 10 条
- [ ] 告警编号格式为 ALT04~ALT07- 日期 - 序号
- [ ] 详情弹窗展示完整告警信息

### 8.3 F03 违法事故统计验收
- [ ] 筛选条件正常工作（行政区域/事故类型/严重程度）
- [ ] 4 个统计卡片数据正确
- [ ] 事故类型分布饼图正常
- [ ] 行政区域柱状图正常
- [ ] 事故趋势折线图正常
- [ ] 事故列表 10 列展示完整，每页默认 10 条
- [ ] 详情弹窗正常展示事故详情和关联信息

### 8.4 F04 运行状态分析验收
- [ ] 维度切换 Tab（按企业/按车型/按行政区域）正常工作
- [ ] 切换维度后图表标题和数据联动更新
- [ ] 5 个统计卡片数据正确
- [ ] 里程对比柱状图和时长分布饼图正常
- [ ] 速度区间分布柱状图 7 段色值正常
- [ ] 数据完整率趋势折线图含均值线
- [ ] 数据采集质量表格每页默认 10 条，支持排序和分页
- [ ] 导出分析报告弹窗正常
- [ ] 车辆运行详情弹窗含近 7 日趋势图

### 8.5 F05-F07 分析类页面验收
- [ ] 预警事件分析单页面正常加载，预警类型枚举为 3 类（车辆运行异常/碰撞事件/车载终端异常），无 ADAS 类型
- [ ] 预警事件分析 6 个统计卡片数据正确
- [ ] 预警类型分布环形图、趋势面积折线图、类型趋势对比多折线图、热点地图 4 个图表正常
- [ ] 预警编号格式为 EW01~EW03- 日期 - 序号
- [ ] 测试里程分析三维度切换和下钻功能正常
- [ ] 产业发展分析图表区展开/收起数据表功能正常

### 8.6 F08-F12 查询类页面验收
- [ ] 企业数据查询两个 Tab 切换正常
- [ ] 企业数据查询两个 Tab 查询条件均为日期范围选择框，开始日期晚于结束日期时提示拦截
- [ ] 企业数据查询列表按日粒度展示：测试查询含示范应用里程/示范应用时长字段，脱手率查询字段为脱手次数
- [ ] 企业数据查询列表首列复选框支持单行勾选与表头全选当页，导出支持导出勾选行（未勾选导出全量）
- [ ] 安全员数据查询筛选条件为日期范围选择框，开始日期晚于结束日期时提示拦截
- [ ] 安全员数据查询列表按日粒度展示（含日期字段），首列复选框支持勾选导出
- [ ] 安全员数据查询筛选和分页正常
- [ ] 车辆牌照查询筛选条件为临牌号/VIN 码/企业名称/车辆类型/牌照状态，牌照状态枚举为有效期内/已过期
- [ ] 车辆牌照查询列表字段含复选框列、有效期起止组合列、历史牌照查看入口，无 " 车型 " 列
- [ ] 车辆测试查询筛选条件为 VIN 码/临牌号/企业名称/日期范围，列表含日期字段与首列复选框，开始/截止时间不再分列展示
- [ ] 车辆脱手率查询筛选条件为 VIN 码/临牌号/企业名称/日期范围，列表含日期字段与首列复选框，开始/截止时间不再分列展示
- [ ] 车辆实时数据查询筛选条件为 VIN 码/临牌号/企业名称/日期范围，列表业务字段为临牌号/VIN 码/所属企业/数据采集时间/经度/纬度/车辆驾驶模式/车辆速度/档位，保留复选框勾选导出和查看详情入口，页面无 " 暂停刷新/自动刷新 " 字样
- [ ] 车辆实时数据查询详情包含基础信息、驾驶与车辆动态、车身信号与操控、远程控制四个分组，支持滚动与全屏查看
- [ ] 车辆作业数据查询筛选条件为 VIN 码/临牌号/企业名称/日期范围，列表含复选框列与油耗/电耗拆分列，表格底部无合计行
- [ ] 设备数据查询筛选条件为 VIN 码/临牌号/企业名称/日期范围/设备编号/在线状态/SIM 卡号，列表含复选框列、安装日期字段，无备注列
- [ ] 车辆故障查询筛选条件为 VIN 码/临牌号/企业名称/发生日期范围/故障码/故障码状态，列表含复选框列与故障等级/所属系统字段，无备注列，故障等级为 1~4 级，故障码状态为已消除/未消除
- [ ] 车辆数据查询 5 个 Tab 切换正常，筛选导出正常
- [ ] 设备数据查询 9 列展示完整
- [ ] 故障信息查询故障状态标签颜色正确

### 8.7 F13 事故数据解析验收
- [ ] 上传事故报告弹窗正常，支持文件选择和关联车辆
- [ ] OCR 解析流程模拟正常（提交→解析中→完成）
- [ ] 详情弹窗分组展示完整信息
- [ ] 人工校正弹窗表单可编辑，保存后状态更新

### 8.8 F14-F16 事故分析类验收
- [ ] 事故成因分析 3 个 Tab 切换正常
- [ ] 片区成因含统计卡片、图表、下钻明细
- [ ] 路段成因左右分栏布局，道路搜索筛选正常
- [ ] 单起成因含时间轴、贡献度饼图、成因详情表
- [ ] 事故特征分析 4 个 Tab 切换正常
- [ ] 事故分析研判 5 个 Tab 切换正常

### 8.9 F17-F19 专题分析验收
- [ ] 道路测试 3 个 Tab 切换正常
- [ ] 企业监测支持多选对比弹窗
- [ ] 车辆详情含测试场景分析、人工接管分析、异常分析
- [ ] 示范应用含服务次数和示范场景分析
- [ ] 示范运营含营收指标和收费分析

### 8.10 业务规则验收
- [ ] 脱手率/干预率计算正确，分母为 0 时显示 "-"
- [ ] 编号格式符合规范（ALT/ACC/EVT 前缀）
- [ ] 时间粒度按筛选范围正确适配
- [ ] 导出上限 10 万条，超限提示
- [ ] 分页默认每页 10 条（查询类 20 条），实时车辆动态 5 条

---

## 附录 A：统一组件规范

> 以下组件在本系统多个模块中复用，功能详细设计中引用这些组件时仅需注明 " 见附录 A.x"。

### A.1 分页组件

| 配置项 | 规则 |
| ---- | ---- |
| 每页条数选项 | 10、20、50、100，默认 10 |
| 显示信息 | 总记录数、当前页码/总页数 |
| 翻页控件 | 首页、上一页、页码（带省略号省略）、下一页、末页 |
| 切换每页条数 | 下拉选择，切换后从第一页重新加载 |

**特殊场景分页配置：**

| 配置项 | 规则 |
| ---- | ---- |
| 实时车辆动态列表 | 默认 5 条/页 |
| 查询类列表（企业/安全员/车辆/设备/故障） | 默认 20 条/页 |

### A.2 删除确认组件

本模块不涉及删除功能。

### A.3 导出组件

| 触发条件 | 行为 |
| ---- | ---- |
| 用户选中了特定数据 | 导出用户选择的数据，不支持跨页导出 |
| 用户未选择特定数据 | 导出当前筛选条件下的全量数据 |

**导出格式：** .xlsx

**文件命名规则：** {功能名称}_{YYYYMMDD}.xlsx

**导出上限：** 10 万条，超限提示 " 数据量过大，请缩小筛选范围后重试 "

### A.4 附件上传组件

| 规则项     | 图片类          | 文档类       |
| ------- | ------------ | --------- |
| 支持格式    | jpg/png      | pdf       |
| 单文件大小限制 | ≤5MB         | ≤20MB     |
| 多文件上传   | 支持（最多 5 张）     | 支持        |
| 预览/下载   | 查看态支持点击预览/下载 | 查看态支持点击下载 |

### A.5 复选框组件
- 表头存在全选复选框，勾选后选中当前页所有行
- 支持单行勾选
- 全选仅作用于当前页数据，不支持跨页全选

### A.6 查询筛选区组件

**通用布局规则：**

- 查询条件与查询按钮、重置按钮在同一行排列
- 查询按钮为主按钮（蓝色），重置按钮为次按钮
- 导出按钮位于筛选行右侧
- 查询项超过默认展示范围时，仅显示高频条件，其他条件通过“展开更多”按需显示
- 展开和收起不改变任一条件值；查询、重置和导出按全部条件执行

### A.7 状态标签规范

| 状态类型 | 颜色 | 色值 |
| ---- | ---- | ---- |
| 待处理/未处理 | 蓝色 | #1677ff |
| 处理中 | 黄色 | #fa8c16 |
| 已完成/已消除/已管控 | 绿色 | #52c41a |
| 严重/高风险/死亡 | 红色 | #ff4d4f |
| 已解析/在线/正常 | 蓝色 | #1677ff |
| 已校正 | 绿色 | #52c41a |
| 待解析/待管控 | 黄色 | #fa8c16 |
| 离线/未知 | 灰色 | #00000040 |
| 事故类型 - 碰撞/追尾/侧翻 | 红色 | #ff4d4f |
| 事故类型 - 行人冲突 | 黄色/#fa8c16 | 刮擦 | 蓝色 | #1677ff |
| 严重程度 - 死亡 | 红色 | #ff4d4f |
| 严重程度 - 重伤 | 黄色 | #fa8c16 |
| 严重程度 - 轻伤 | 蓝色 | #1677ff |
| 严重程度 - 无伤亡 | 绿色 | #52c41a |
| 事故分类 - 已上报 | 黄色 | #fa8c16 |
| 事故分类 - 已提交交通事故报告 | 蓝色 | #1677ff |
| 事故分类 - 已提交分析报告 | 绿色 | #52c41a |
| 责任认定 - 全责/主责 | 红色 | #ff4d4f |
| 责任认定 - 同责 | 黄色 | #fa8c16 |
| 责任认定 - 次责 | 蓝色 | #1677ff |
| 责任认定 - 无责 | 绿色 | #52c41a |
| 责任认定 - 待认定 | 灰色 | #00000040 |
| 预警事件 - 车辆运行异常预警 | 蓝色 | #1677ff |
| 预警事件 - 车辆碰撞事件预警 | 橙色 | #fa8c16 |
| 预警事件 - 车载终端异常预警 | 紫色 | #722ed1 |
| 告警事件 - 临牌有效期告警 | 蓝色 | #1677ff |
| 告警事件 - 电子围栏越界告警 | 橙色 | #fa8c16 |
| 告警事件 - 测试时段合规性告警 | 紫色 | #722ed1 |
| 告警事件 - 安全员到岗状态告警 | 青色 | #13c2c2 |
