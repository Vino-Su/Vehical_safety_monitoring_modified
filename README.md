# 智能网联汽车安全监测平台 — 前端高保真原型

## 项目简介

本项目是**智能网联汽车安全监测平台**的前端高保真原型，面向襄阳市智能网联汽车道路测试与示范应用的监管场景，用于产品评审、需求落地与开发参考。

原型覆盖**事前准入、事中监控、事后评价**全流程，包含 4 大业务模块、56 个功能页面，所有页面严格复刻 Ant Design 企业级后台视觉规范。当前为纯前端静态原型，无后端接口，数据均为模拟数据。

## 近期更新

- 2026-08-31：临时牌照登记流程新增第三方专业管理机构审核节点。申请主体在牌照弹窗中按 VIN、型号、临牌号、有效期、照片逐车登记并提交审核；系统校验临牌号全平台唯一，第三方可通过或不通过，不通过时回传修改意见，申请主体修改后重新提交，审核通过后申请才生效。同步更新测试示范监管模块 PRD V4.7。

- 2026-08-26：道路评估风险模型明确“交通事件频率”唯一取自交通违法信息；按道路统一计数、不区分违法类别，按统计时间窗计算月均频率（起/月），无法匹配道路的记录不计入。

- 2026-08-26：平台管理新增**权限管理**子模块（6 个页面，仅系统管理员可见）：账号用户管理（账号全生命周期、批量导入、角色分配与复制他人配置，不提供物理删除）、组织架构管理（组织树维护与成员查看，作为数据权限过滤基准）、角色管理（系统内置/自定义角色、分配用户、角色复制、被引用删除拦截）、角色授权（左侧角色列表 + 应用→菜单→按钮/接口权限树勾选，默认查看模式、编辑后保存即时生效，内置角色只读）、权限资源管理（以权限树形式维护，应用节点下新增菜单、菜单节点下新增子资源，含应用管理与批量导入）、数据权限管理（角色/用户数据范围五档预置 + 自定义业务规则，用户级优先、多角色并集，最终生效范围预览）。同步新增《权限管理PRD》（V1.1）。
- 2026-08-25：开放道路地图可视化新增电子围栏叠加图层，围栏按状态区分显示；右侧资源面板增加“道路/电子围栏”Tab，支持围栏名称、类型、状态列表、筛选、搜索、地图定位与信息提示。
- 2026-08-24：F08 第三方初审支持针对单个申请材料填写文本批注，退回时自动生成材料问题清单并提供给申请主体；退回重提后第三方初审可查看只读历史批注，专班审核、专家评审和专题会审议不展示批注入口，相关记录通过流程日志追溯。道路测试、示范应用、商业化试点及延期、变更、新增车辆详情统一接入批注结果查看组件。
- 2026-08-24：异常状态事件新增企业处置与第三方审核闭环。企业用户通过独立“异常事件处置”页面对本企业七类事件填写处理说明并提交审核；第三方专业服务机构在监管侧事件详情中审核通过或退回补充，处理状态统一为待处理、待审核、已退回、已完成，并保留完整流程日志。
- 2026-08-24：道路档案统一使用“适用应用场景”（无人物流、无人环卫、无人公交、无人出行），准入申请新增必填“应用场景”；申请选路时按道路适用应用场景过滤并在确认/提交前阻断不匹配道路，申请记录、审批详情、地图和导入导出同步更新。道路测试、示范应用、商业化试点及其派生申请、审批详情中的道路信息弹窗统一删除测试场景信息，并在开放管控信息中展示适用业务类型。
- 2026-08-24：道路信息删除“测试场景信息”分组及相关字段，在“开放管控信息”增加“适用业务类型”（道路测试、示范应用、商业化试点）；道路资源动态管控的准入配置支持适用应用场景、适用业务类型多选并移除“测试场景类型”。

- 2026-08-18：企业、人员、车辆详情统一显示快照编号、快照时间和修改人；信息管理入口可通过标题栏“信息快照”查看对象历史，准入申请详情则仅打开本申请提交时固化的对象快照，不提供历史快照切换，确保审批依据可追溯且不受后续登记修改影响。

- 2026-08-18：角色菜单与交付权限表完成对齐。补齐第三方专业服务机构、市工作专班、交通管理部门及企业用户的数据查询页面范围；首页功能导航改为复用角色菜单配置，确保首页、顶栏、侧栏与直接访问校验一致。本次仅处理页面级访问范围，不涉及数据范围与按钮级权限。

- 2026-08-17：道路测试、示范应用和商业化试点的变更详情统一复用变更申请表格语义。车辆、驾驶员（安全员）、道路或区域在申请详情和审批详情中均以提交时固化的全量对象快照展示保留、新增、拟删除行级状态；标量字段继续展示关联原申请的实际原值与变更后值，避免对象汇总文本与明细不一致。

- 2026-08-17：在线反馈统一为待处理、处理中、已完成三态。管理侧取消独立关闭流程，处理中记录始终可回复，并通过“提交并继续处理”或“提交并完成”驱动状态流转；公众侧统一按三态查看处理进度和回复内容。

- 2026-08-17：道路测试、示范应用和商业化试点申请统一为轻量的临时牌照登记流程。初次申请、涉及新增或替换车辆的变更申请、以及新增相同配置车辆申请在审批通过后登记本次车辆的牌照号和有效期；延期及不涉及车辆的变更直接生效。申请列表与详情车辆信息复用同一登记入口，不新增待办、通知、凭证上传或独立页面；全部车辆完成校验后自动生效，已生效详情只读展示牌照信息。

- 2026-08-24：交通违法信息新增批量导入功能，支持下载导入模板与 Excel/CSV 文件上传（单次最大1000条），必填字段与列表字段对齐，导入后自动校验并提示；批量导入按钮对全部角色可见，确认导入结果以全局提示反馈。
- 2026-08-24：企业异常事件处置查询区新增发生时间范围筛选并移除处理状态下拉；统计卡片补充"全部"卡片并支持点击按处理状态筛选列表，默认选中"全部"，卡片数量随查询条件联动，重置后恢复全部展示。
- 2026-08-24：临时牌照登记新增牌照照片必传（JPG/PNG 格式），有效期由单一截止日期改为起止时间范围；共享登记组件（道路测试/示范应用准入/示范应用运营的「登记临时牌照」弹窗）按行登记牌照号、有效期起止和照片并校验起止顺序与照片必传，登记列表移除生产企业/品牌列，照片上传后以缩略图展示并支持点击弹窗预览大图，道路测试批量牌照上传与单车更新牌照弹窗同步改造。

- 2026-08-14：道路测试、示范应用和商业化试点的延期申请及详情统一接入共享框架。延期入口统一在原有效期结束前 3 个月开放，自动接续原截止日次日，单次延期不得超过 6 个月；表单统一校验新结束日期、延期原因及对应业务的最新安全性自我声明、延期申请书、前期总结评估报告和延期必要性说明。派生详情统一采用“顶部摘要—申请信息—申请材料—原申请内容（默认收起）—审批流程进度—流程日志”骨架，原申请附件仅在原申请内容内按材料目录直接展示；原申请内容直接关联初次申请记录，并分别以道路测试、示范应用、商业化试点的初次申请标题展示路段、车辆和人员明细表。新建派生申请复用前四段骨架，原申请内容默认收起，提交前不展示审批进度和流程日志。详情标题中的申请状态使用标签展示，初次申请基本信息不再重复展示申请状态，不调整初次申请表单。

- 2026-08-14：道路测试、示范应用和商业化试点的新增相同配置车辆申请及详情统一接入共享框架。本次申请材料统一为对应业务的最新安全性自我声明和新增相同配置车辆必要性说明；原申请附件按实际初次申请材料分组只读关联。所有查看态材料统一按道路测试初次申请详情的材料目录展示编号、名称、文件或关联档案及预览、下载操作。新增车辆通过本地清单选择，校验与原申请的车型、自动驾驶级别一致且未重复纳入；详情展示车辆、材料、审批流程和版本演进，不单独展示配置核验结果。

- 2026-08-13：道路测试、示范应用和商业化试点变更申请及详情统一接入共享框架。变更表单仅保留申请主体只读，按业务类型提供七项可勾选变更字段；车辆、驾驶员（安全员）、路段或区域勾选后以原申请清单预填表格，复用既有选择弹窗新增，删除改为可恢复的拟删除。保留、新增和拟删除对象在同一表格中以行状态展示，提交时固化对象快照；变更详情提供原值/新值对比及同规格对象表格，供审批人员审查。统一使用变更原因文本框、原声明只读关联及三份本次上传材料；右侧“版本演进”按钮打开独立弹窗。附件区对齐延期申请样式，原申请附件则按各自初次申请材料清单完整只读关联。

- 2026-08-12：审批管理详情与道路测试、示范应用、商业化试点申请详情同步字段及附件结构；审批人员可查看完整道路/区域、车辆、人员档案和历史牌照，审批端保持只读查看并仅提供整体通过或退回。
- 2026-08-10：设备数据查询、车辆故障查询、在线反馈管理、异常状态事件、交通违法信息和数据字典管理的查询区改为默认展示高频条件，其余条件通过“展开更多”按需显示；展开和收起不清除已填条件，查询与重置覆盖全部条件。
- 2026-08-10：异常状态事件支持按事件分类、企业简称、临牌号、VIN码、处理状态与秒级发生时间范围查询；统计卡片、各事件列表和 CSV 导出按当前筛选结果联动。事件详情补齐命中规则和分类型实时快照，并可将车辆与事件前20秒至后10秒的时间范围带入车辆实时数据查询、监控一张图轨迹和视频回放。
- 2026-08-08：监控一张图在全市视角按行政区聚合车辆，展示车辆、在线、预警和告警统计；聚合锚点与示例车辆按高新区、樊城区、襄城区、襄州区的实际空间关系分布，点击区域聚合结果可下钻至单车分布，车辆列表、搜索和异常事件入口仍可直接定位并放大目标车辆。
- 2026-08-05：监控一张图车辆信息的异常事件卡片新增“回放”，关闭信息窗后同步开启事件关联车辆的轨迹与视频回放，回放窗口覆盖异常发生前 1 分钟至发生时刻，并在地图左上角保留事件摘要；点击“退出回放”后关闭轨迹回放、恢复视频实时态并切回异常事件视图。
- 2026-08-05：监控一张图轨迹回放新增“视频回看”，可一键切换右侧视频面板并同步车牌、查询日期、时间范围与当前播放指针。
- 2026-08-05：信息发布管理补充待审核信息的通过/退回审核、审核意见与详情流转时间线；已发布信息改为“下架”，下架后进入“已下架”。

## 快速开始

### 环境要求

- 现代浏览器（Chrome / Edge / Firefox 最新版）
- 无需 Node.js、无需构建工具、无需安装任何依赖

### 运行方式

**方式一：直接打开**

双击项目根目录下的 `index.html`，浏览器会自动跳转到首页工作台。

**方式二：本地 HTTP 服务（推荐）**

部分功能（如 Leaflet 地图、ECharts 图表）通过 CDN 加载资源，使用 HTTP 服务可避免跨域问题：

```bash
# Python 3
python -m http.server 8080

# Node.js（需先安装 serve）
npx serve .

# PowerShell
php -S localhost:8080
```

然后访问 `http://localhost:8080`。

**方式三：VS Code Live Server**

在 VS Code 中安装 Live Server 扩展，右键 `index.html` → Open with Live Server。

**方式四：GitHub Pages**

本项目已适配 GitHub Pages 部署，推送至 GitHub 仓库后启用 Pages 即可通过公开链接访问。

## 技术架构

### 技术栈

| 层面 | 技术 | 说明 |
|------|------|------|
| 标记语言 | HTML5 | 语义化结构，每个功能页面独立 HTML 文件 |
| 样式框架 | Tailwind CSS v3（CDN） | 原子化 CSS，配合自定义 Ant Design 色值 |
| 交互逻辑 | 原生 JavaScript（ES5） | 无框架依赖，所有逻辑直接写在页面 `<script>` 中 |
| 图表库 | ECharts 5.4.3（CDN） | 统计分析、趋势折线、饼图、柱状图等 |
| 地图库 | Leaflet 1.9.4（CDN） | 道路可视化、监控一张图等地图页面 |
| 图标库 | Font Awesome 6（CDN） | 部分页面引入 |

### 整体架构

```
index.html                          ← 入口，自动跳转
03-高保真页面/
├── layout.html                     ← 首页工作台（统计卡片 + 最近准入申请 + 待办 + 公告 + 功能导航）
├── common.js                       ← 全局公共模块（模块导航 / 侧栏 / 样式 / 弹窗 / 下拉框）
├── components/
│   └── pagination-component.js     ← 可复用分页组件
├── 流程图/                         ← 平台业务链路与页面关系图
│   ├── page-relation-map.html      ← 可交互关系图页面
│   ├── page-relation-data.js       ← 页面节点与关联关系配置
│   └── page-relation-map.css       ← 关系图样式
├── road/                           ← 开放道路资源管理（7 页）
├── monitor/                        ← 测试示范监管（27 页）
├── data-analysis/                  ← 监测数据分析（19 页）
└── platform/                       ← 平台管理（2 页）
```

### 页面骨架

每个功能页面遵循统一结构：

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <script src="https://cdn.tailwindcss.com"></script>
  <!-- 按需引入 ECharts / Leaflet / Font Awesome -->
  <script>tailwind.config = { theme: { extend: { colors: { primary: { DEFAULT: '#1677ff' } } } } }</script>
</head>
<body>
<div id="app">
  <header id="topnav"></header>           ← 管理端：品牌 + 一级模块 + 全局操作
  <div id="admin-body">
    <aside id="sidebar"></aside>          ← 当前一级模块的二、三级导航
    <div class="flex-1 flex flex-col overflow-hidden">
    <main class="flex-1 overflow-auto bg-[#f0f2f5] p-6">
      <!-- 页面主体内容 -->
    </main>
    </div>
  </div>
</div>
<script>
var PAGE_ID = 'xxx';   // 当前页面标识，common.js 据此同步模块与功能菜单状态
// 页面专属逻辑...
</script>
<script src="./common.js"></script>       ← 必须在页面脚本之后引入
</body>
</html>
```

### 核心公共模块：common.js

`common.js` 是整个平台的骨架，在 `DOMContentLoaded` 时自动执行以下工作：

| 职责 | 说明 |
|------|------|
| **侧边导航栏** | 根据当前一级模块渲染二级分组与三级功能，支持分组展开/收起、整体折叠和当前功能高亮 |
| **顶部导航栏** | 包含平台品牌、按角色过滤的一级模块、通知图标和用户信息；用户菜单保留退出登录入口占位 |
| **全局 CSS 注入** | 注入 Ant Design 风格的按钮、表格、标签、弹窗、分页、表单、筛选栏等组件样式 |
| **弹窗系统** | 提供 `openModal(title, bodyHTML, options)` / `closeModal()` 全局方法，支持嵌套弹窗栈、自定义宽度、点击遮罩关闭 |
| **下拉框自动转换** | 扫描页面中 `.ant-select` 占位元素，根据 `SELECT_OPTIONS_MAP` 映射表自动替换为真实 `<select>` 元素并填充选项 |
| **页面语境管理** | 根据 `PAGE_ID` 同步一级模块与功能菜单状态；管理端不显示路径式面包屑和独立页面标题，公众角色保持独立公开导航 |

**关键全局变量**：

- `PAGE_ID`：每个页面必须定义，用于模块导航和功能菜单高亮
- `MENU_DATA`：三级菜单配置（一级模块 / 二级分组 / 三级功能，含 key / label / path / icon）
- `SELECT_OPTIONS_MAP`：下拉框选项映射表（键名为筛选标签文本，值为选项数组）
- `openModal()` / `closeModal()`：弹窗方法

### 分页组件：pagination-component.js

提供可复用的 `Pagination` 类，使用方式：

```javascript
var pager = new Pagination({
  container: 'paginationBar',     // 容器元素 ID
  pageSize: 10,                   // 每页条数（默认 10）
  pageSizeOptions: [10, 20, 50],  // 可切换选项
  onPageChange: function(page, size) {
    renderMyList(page, size);     // 页码变化回调
  }
});
pager.setTotal(100);              // 设置总条数，自动刷新分页栏
pager.reset();                    // 重置到第一页
```

## 业务模块说明

### 模块总览

| 模块 | 菜单 Key | 目录 | 页面数 | PRD 文档 |
|------|----------|------|--------|----------|
| 开放道路资源管理 | `road` | `03-高保真页面/road/` | 7 | `02-产品文档/开放道路管理模块PRD.md`、`02-产品文档/城市道路开放目录PRD.md`（分组级） |
| 测试示范监管 | `monitor` | `03-高保真页面/monitor/` | 21 | `02-产品文档/测试示范监管模块PRD.md` |
| 监测数据分析 | `analysis` | `03-高保真页面/data-analysis/` | 21 | `02-产品文档/监测数据分析模块PRD.md` |
| 平台管理 | `platform` | `03-高保真页面/platform/` | 2 | `02-产品文档/平台管理模块PRD.md` |

### 开放道路资源管理（7 页）

| 子模块 | 页面 | PAGE_ID |
|--------|------|---------|
| 城市道路开放目录 | 道路信息建档 | `filing` |
| | 道路资源动态管控 | `control` |
| 道路资源可视化 | 地图可视化展示 | `road-map` |
| 开放道路评估管理 | 评估结果管理 | `eval-result` |
| | 评估指标配置 | `eval-config` |
| | 评估日志管理 | `eval-log` |
| 区域统计查询 | 区域统计查询 | `stats` |

### 测试示范监管（23 页）

| 子模块 | 页面 | PAGE_ID |
|--------|------|---------|
| 准入信息管理 | 企业信息登记 | `company` |
| | 车辆信息登记 | `vehicle` |
| | 人员信息登记 | `person` |
| 准入申请 | 道路测试准入申请 | `road-test` |
| | 示范应用准入申请 | `demo-apply` |
| | 商业化试点准入申请 | `demo-operate` |
| | 业务申请记录 | `apply-records` |
| | 报告提交管理 | `report-submission` |
| 准入审批管理 | 审批管理 | `approve` |
| | 资格终止管理 | `terminate` |
| | 报告管理 | `report` |
| 自定义配置 | 测试示范审批配置 | `approval-config` |
| | 材料目录配置 | `approve-config` |
| | 异常事件规则配置 | `abnormal-rule-config` |
| 车辆状态监控 | 监控一张图 | `monitor-map` |
| | 车辆运行监控 | `running` |
| | 异常状态事件 | `alarm` |
| | 异常事件处置 | `alarm-handle` |
| | 事故数据上报 | `accident` |
| | 事故审核 | `accident-review` |
| 测试服务管理 | 信息发布管理 | `notice` |
| | 在线反馈管理 | `feedback-manage` |
| | 电子围栏管理 | `fence` |
| | 测试评价管理 | `evaluation` |
| 信息公开与在线反馈 | 信息公开 | `info` |
| | 在线反馈 | `feedback` |

### 监测数据分析（21 页）

| 子模块 | 页面 | PAGE_ID |
|--------|------|---------|
| 测试数据分析 | 测试数据总览 | `overview` |
| | 异常告警统计 | `alarm-stats` |
| | 违法事故统计 | `violation-stats` |
| | 运行状态分析 | `running-analysis` |
| | 预警事件分析 | `warning-analysis` |
| | 测试里程分析 | `mileage-analysis` |
| | 产业发展分析 | `industry-analysis` |
| 数据查询检索 | 企业数据查询 | `company-query` |
| | 安全员数据查询 | `safety-driver-query` |
| | 车辆数据查询 | `vehicle-query` |
| | 设备数据查询 | `device-query` |
| | 车辆故障查询 | `fault-query` |
| 事故数据分析 | 事故数据解析 | `accident-parse` |
| | 事故成因分析 | `accident-cause` |
| | 事故特征分析 | `accident-feature` |
| | 事故分析研判 | `accident-judge` |
| 测试示范分析 | 道路测试分析 | `road-test-analysis` |
| | 示范应用分析 | `demo-apply-analysis` |
| | 商业化试点分析 | `demo-operate-analysis` |

### 平台管理（2 页）

| 子模块 | 页面 | PAGE_ID |
|--------|------|---------|
| 数据字典管理 | 字典项管理 | `dict-manage` |
| 操作日志管理 | 日志查询 | `log-manage` |

## 角色与页面权限

| 角色 | 可访问页面范围 |
|------|----------------|
| 系统管理员 | 全部页面 |
| 企业用户 | 准入信息管理、准入申请、车辆状态监控（不含监管侧异常状态、事故审核；含异常事件处置）、信息公开与在线反馈、数据查询检索 |
| 第三方专业服务机构 / 市工作专班 | 开放道路资源、准入信息与审批、自定义配置、车辆监控、测试服务、信息公开及监测数据分析 |
| 交通管理部门 | 指定车辆监控页、事故审核与事故数据分析 |
| 公众访客 | 信息公开与在线反馈 |

> 详见 `02-产品文档/不同人员页面.md`

## 如何修改

### 新增页面

1. 在 `03-高保真页面/` 下对应模块目录中创建新 HTML 文件
2. 复制任意现有页面作为模板，修改 `<title>` 和页面主体内容
3. 在页面 `<script>` 中定义 `var PAGE_ID = 'xxx'`
4. 在 `common.js` 的 `MENU_DATA` 中添加菜单项（含 key / label / path）
5. 如有新的筛选下拉选项，在 `common.js` 的 `SELECT_OPTIONS_MAP` 中添加映射
6. 如需分页，引入 `pagination-component.js` 并初始化 `Pagination` 实例

### 修改现有页面

1. 直接编辑对应 HTML 文件中的页面主体区域（`<main>` 标签内）
2. 顶部模块导航、侧栏和页面操作入口由 `common.js` 自动处理，无需重复修改页面壳层
3. 弹窗内容通过 `openModal(title, bodyHTML)` 调用，修改对应函数中的 `bodyHTML` 即可

### 修改导航菜单

编辑 `common.js` 中的 `MENU_DATA` 数组，结构为三级树形：

```javascript
{
  key: 'unique-key',        // 唯一标识，与 PAGE_ID 对应
  label: '菜单显示名称',
  icon: '<svg>...</svg>',   // 仅一级菜单需要
  children: [               // 子菜单
    {
      key: 'sub-key',
      label: '二级菜单',
      children: [
        { key: 'leaf-key', label: '三级菜单', path: '模块/子目录/页面.html' }
      ]
    }
  ]
}
```

### 修改全局样式

- **组件级样式**（按钮、表格、标签等）：编辑 `common.js` 中 `injectStyles()` 函数内的 `cssText` 数组
- **设计规范**（颜色、字体、间距）：参照 `.claude/UI_design.md` 中的规范定义
- **页面局部样式**：在页面 `<style>` 标签中编写，注意避免与全局样式冲突

### 修改下拉框选项

编辑 `common.js` 中的 `SELECT_OPTIONS_MAP` 对象，键名为筛选标签文本（如 `'所属区域'`），值为选项数组：

```javascript
'所属区域': ['全部', '樊城区', '襄城区', '襄州区', ...],
```

### 使用分页组件

```html
<!-- HTML 中放置容器 -->
<div id="paginationBar"></div>

<!-- 引入组件脚本 -->
<script src="../../components/pagination-component.js"></script>

<!-- 初始化 -->
<script>
var pager = new Pagination({
  container: 'paginationBar',
  pageSize: 10,
  onPageChange: function(page, size) {
    // 根据 page 和 size 过滤/切片数据，重新渲染表格
  }
});
pager.setTotal(data.length);
</script>
```

### 使用弹窗

```javascript
// 基本弹窗
openModal('标题', '<p>内容HTML</p>');

// 宽弹窗（800px）
openModal('标题', '<p>内容</p>', { wide: true });

// 自定义宽度
openModal('标题', '<p>内容</p>', { width: 720 });

// 自定义底部按钮
openModal('标题', '<p>内容</p>', {
  footer: '<button class="ant-btn" onclick="closeModal()">取消</button>' +
          '<button class="ant-btn ant-btn-primary" onclick="doSomething()">确认</button>'
});

// 弹窗打开后回调（用于初始化弹窗内组件）
openModal('标题', '<div id="chartInModal"></div>', {
  wide: true,
  onOpen: function() {
    // 初始化 ECharts 图表等
  }
});
```

## UI 设计规范

所有页面严格遵循 `.claude/UI_design.md` 中定义的 Ant Design 企业级后台规范，核心要点：

| 项目 | 规范 |
|------|------|
| 主色 | `#1677ff`（标准蓝），hover `#4096ff`，禁用 `#85a5cc` |
| 中性色 | 文字 `#000000d9` / `#00000073` / `#00000040`，背景 `#f0f2f5`，分割线 `#f0f0f0` |
| 字体 | `"Microsoft YaHei", -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` |
| 字号 | 页面标题 20px，模块标题 16px，正文 14px，辅助 12px |
| 侧边栏 | 224px 宽，浅色背景，仅展示当前一级模块下的二级和三级功能 |
| 顶栏 | 64px 高，白底，含平台品牌、一级模块、通知和用户信息 |
| 表格 | 表头行高 48px，数据行高 48px，单元格内边距 16px |
| 弹窗 | 表单弹窗 600px，详情弹窗 800px，最大 90vw |
| 输入框 | 高 32px，圆角 6px，内边距 0 12px |
| 按钮 | 常规 32px 高，小按钮 24px 高，圆角 6px |

## 项目目录结构

```
智能网联汽车安全监测平台/
├── index.html                          ← 入口文件，跳转至 03-高保真页面/layout.html
├── README.md                           ← 本文件
├── .gitignore
├── .gitattributes
│
├── .claude/                            ← Claude Code 工作区配置
│   ├── CLAUDE.md                       ← 项目工作规范
│   ├── UI_design.md                    ← UI 设计规范
│   ├── commands/opsx/                  ← 自定义命令
│   └── skills/                         ← 自定义技能
│
├── 01-需求文档/                         ← 需求文档（开发的核心依据）
│   ├── 开放道路管理模块.md
│   ├── 测试示范监管模块.md
│   ├── 监测数据分析模块.md
│   └── 平台管理模块.md
│
├── 02-产品文档/                         ← PRD 产品需求文档
│   ├── 开放道路管理模块PRD.md           ← V1.1
│   ├── 城市道路开放目录PRD.md           ← V1.0（分组级：建档 + 动态管控）
│   ├── 测试示范监管模块PRD.md           ← V3.7
│   ├── 监测数据分析模块PRD.md           ← V1.1
│   ├── 平台管理模块PRD.md              ← V1.1（已确认）
│   └── 不同人员页面.md                 ← 角色页面权限说明
│
├── 03-高保真页面/                       ← 前端原型源码（核心目录）
│   ├── layout.html                     ← 首页工作台
│   ├── common.js                       ← 全局公共模块
│   ├── components/
│   │   └── pagination-component.js     ← 分页组件
│   ├── road/                           ← 开放道路资源管理
│   │   ├── catalog/                    ← 城市道路开放目录
│   │   ├── visualization/              ← 道路资源可视化
│   │   ├── evaluation/                 ← 开放道路评估管理
│   │   └── statistics/                 ← 区域统计查询
│   ├── monitor/                        ← 测试示范监管
│   │   ├── access-info/                ← 准入信息管理
│   │   ├── access-apply/               ← 准入申请
│   │   ├── access-approve/             ← 准入审批管理
│   │   ├── approve-config/             ← 自定义配置
│   │   ├── vehicle-monitor/            ← 车辆状态监控
│   │   ├── service/                    ← 测试服务管理
│   │   └── public/                     ← 信息公开与在线反馈
│   ├── data-analysis/                  ← 监测数据分析
│   │   ├── analysis/                   ← 测试数据分析
│   │   ├── query/                      ← 数据查询检索
│   │   ├── accident/                   ← 事故数据分析
│   │   └── demo/                       ← 测试示范分析
│   └── platform/                       ← 平台管理
│       ├── dict/                       ← 数据字典管理
│       └── log/                        ← 操作日志管理
│
├── 05-参考文件/                         ← 参考文件与申报规程
│   ├── 附则1：襄阳市智能网联车辆道路测试与示范应用申报规程.docx
│   ├── 附则1-2：重点应用场景进阶流程图.pptx
│   ├── 襄阳市智能网联车辆道路测试与示范应用申报规程.md
│   ├── 道路测试_示范应用_商业化试点申请对比分析.md
│   ├── 三类申请材料汇总与对比.md
│   └── PRD与参考文件差异对比清单.md
│
├── 06-备份/                             ← 最近一次任务执行前的备份
│   ├── 01-需求文档/
│   ├── 02-产品文档/
│   └── 03-高保真页面/
│
└── 07-bugs/                            ← 自测截图（待创建）
```

## 关键交互模式

| 场景 | 实现方式 |
|------|----------|
| 列表页 | 筛选栏（`.filter-bar`）+ 表格（`.ant-table`）+ 分页（`Pagination`）+ 导出按钮 |
| 弹窗 | `openModal()` 全局方法，支持嵌套栈、自定义宽度、底部按钮 |
| 状态标签 | `.ant-tag-success` / `.ant-tag-warning` / `.ant-tag-error` / `.ant-tag-processing` / `.ant-tag-default` |
| 审批流程 | 时间线组件，串联审批节点：第三方初审 → 市工作专班审核 → 专家评审 → 专班专题会审议 |
| 图表页面 | ECharts 渲染，支持时间筛选（快捷按钮 + 日期范围）、维度切换 |
| 地图页面 | Leaflet / Canvas 渲染，支持图层切换、搜索定位、信息窗弹窗 |
| 下拉框 | 页面中写 `.ant-select` 占位元素 + 显示文本，`common.js` 自动转换为 `<select>` 并填充选项 |

## 开发注意事项

1. **CDN 依赖**：Tailwind CSS、ECharts、Leaflet、Font Awesome 均通过 CDN 加载，开发时需联网
2. **PAGE_ID 必须定义**：每个页面必须定义 `var PAGE_ID`，且其值必须与 `MENU_DATA` 中某个叶子节点的 `key` 一致，否则无法同步当前模块和功能菜单状态
3. **common.js 引入顺序**：必须在页面 `<script>` 之后引入，确保 `PAGE_ID` 先于 `common.js` 执行
4. **路径计算**：`common.js` 通过 `getBasePath()` 自动计算相对路径深度，页面在子目录下任意层级均可正确导航
5. **弹窗嵌套**：`openModal()` 支持多层嵌套，内层弹窗打开时外层自动隐藏，关闭内层后外层自动恢复
6. **分页默认**：所有列表页默认每页 10 条，支持分页
7. **备份机制**：修改超过 3 个文件或生成新版本时，自动备份至 `06-备份/` 目录，保留最近 3 次
8. **需求对齐**：所有页面需与 `01-需求文档/` 和 `02-产品文档/` 中的文档保持一致，不得遗漏功能点
9. **UI 规范**：所有页面需遵从 `.claude/UI_design.md` 中的设计规范，包括颜色、字体、图标等
10. **二级页面与弹窗**：页面中逻辑上应存在的二级页面、弹窗等也需绘制，即使需求文档未明确要求
11. **术语对齐**：示范运营已统一为 " 商业化试点 "，第三方服务机构已统一为 " 第三方专业管理机构 "，联席工作小组已统一为 " 市工作专班 "

## 更新日志

### 2026-08-13 - 分页栏统一接入公共分页组件

- 审批管理（approve.html）分页栏位置调整为表格右下角。
- 车辆信息管理、人员信息查询、企业/安全员/车辆/设备/车辆故障数据查询、事故数据解析共 8 个页面的表格分页栏统一替换为公共分页组件 `components/pagination-component.js`，位置统一为表格右下角。
- 业务总览、在线反馈、信息公开、道路测试/示范应用/商业化试点专题分析、事故特征分析、事故分析研判、评估报告、运行状态分析、测试里程分析、车辆监控地图、道路测试申请弹窗等 14 个页面的旧式分页（静态页码占位、自写上一页/下一页、假翻页提示）一并统一替换为公共分页组件；事故研判 5 个动态表、评估报告、运行/里程分析 328 条数据、监控地图车辆列表等接真实分页逻辑，删除了各页空壳 changePageSize 函数与死 CSS。
- 事故数据解析自写分页逻辑（currentPage/pageSize/onclick）迁移至公共组件；车辆数据查询的牌照/实时数据表接入真实分页（slice + setTotal），静态表按实际行数展示；各页面原静态页码占位与重复的“共 N 条”“每页条数”控件一并移除。

### 2026-08-12 - 准入申请版本演进收敛

- 道路测试、示范应用、商业化试点三类准入申请统一采用“资格主链 + 新增车辆附属分支”展示完整申请关系；主链按申请时间排序，新增车辆不进入资格主链。
- 版本演进节点仅使用状态语义，延期/变更/新增车辆通过中性关系文字表达；申请编号支持跳转详情，变更字段与车辆来源仍在各自业务区域展示。

### 2026-08-11 - 三类准入申请、审批附件规则调整

- 道路测试初次申请及审批详情统一按“附件1-3：申请文件”“附件4：申请材料”“自动驾驶道路测试补充材料”组织材料；安全性自我声明作为附件3单独上传。
- 附件4调整为规程原有16项顺序，营业执照自动关联企业档案，安全员材料关联已选人员档案，不要求重复上传；每项简称均可查看对应规程完整原文。
- 审批保留附件预览、下载与整体通过/退回，不新增材料状态、核验状态或单项材料审核。
- 示范应用初次申请及审批详情统一按“附件1/6/7：申请文件”“附件8：申请材料”“自动驾驶示范应用补充材料”组织材料；附件8调整为规程原有13项顺序，驾驶员/安全员材料关联已选人员档案。
- 示范应用模式切换仅展示前置条件：主驾有人为480小时/1000公里道路测试，自动驾驶为480小时/2000公里远程道路测试，均要求无交通违法和无由道路测试车辆方承担责任的交通事故；不要求手工录入或勾选确认。
- 商业化试点初次申请及审批详情统一按“附件1/9/10：申请文件”“附件11：申请材料”“商业化试点安全保障方案”组织材料；附件11调整为规程原有12项顺序，驾驶人/安全员材料关联已选人员档案，安全保障方案独立提交并附专业论证和安全风险评估材料。
- 商业化试点模式切换仅展示规程第三十一条、第三十二条前置条件，不提供手工时长、里程录入或确认勾选，也不增加无规程依据的自动驾驶额外材料。
- 道路测试、示范应用、商业化试点的延期、变更和新增相同配置车辆申请均增加“最新安全性自我声明”上传，并在申请详情和审批详情中同步展示；原申请附件保持关联可查看，不被派生申请直接覆盖。

### 2026-08-05 - 管理端导航与页面语境改版

- 管理端调整为顶部一级模块、侧边二三级功能的导航结构；面包屑和独立页面标题不再展示。
- 原页面操作入口按筛选栏、Tab 分组或独立工具栏重新归位；公众角色继续采用独立公开导航。
- 管理端侧栏恢复深色主题并支持整体折叠；平台名称不再使用前置图标，用户菜单增加退出登录占位入口。

## 文档索引

### 需求文档

| 文档 | 路径 | 内容概要 |
|------|------|----------|
| 开放道路管理模块 | `01-需求文档/开放道路管理模块.md` | 道路建档字段清单、动态管控状态变更校验、地图可视化图层、评估模型配置、区域统计 |
| 测试示范监管模块 | `01-需求文档/测试示范监管模块.md` | 准入信息登记字段、三类准入申请流程与材料清单、串联审批流程、监控一张图、电子围栏、测试评价 |
| 监测数据分析模块 | `01-需求文档/监测数据分析模块.md` | 测试数据总览、告警统计 5Tab 架构、数据查询检索、事故解析/成因/特征/研判、测试示范专题分析 |
| 平台管理模块 | `01-需求文档/平台管理模块.md` | 数据字典 CRUD 与版本管理、操作日志查询与审计、日志详情弹窗字段 |

### PRD 文档

| 文档 | 路径 | 版本 | 状态 |
|------|------|------|------|
| 开放道路管理模块 PRD | `02-产品文档/开放道路管理模块PRD.md` | V2.2 | 待确认 |
| 城市道路开放目录 PRD | `02-产品文档/城市道路开放目录PRD.md` | V1.0 | 待确认 |
| 测试示范监管模块 PRD | `02-产品文档/测试示范监管模块PRD.md` | V4.2 | 待确认 |
| 监测数据分析模块 PRD | `02-产品文档/监测数据分析模块PRD.md` | V1.4 | 待确认 |
| 平台管理模块 PRD | `02-产品文档/平台管理模块PRD.md` | V1.2 | 已确认 |
| 角色页面权限 | `02-产品文档/不同人员页面.md` | — | — |
